/* ============================================
   RESET & BASE STYLES
   ============================================ */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: 'Poppins', sans-serif;
    background: #000;
    color: #ffffff;
    overflow: hidden;
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
}

/* ============================================
   GLOBAL SIZE REDUCTION (10-15%)
   ============================================ */
h1 { font-size: clamp(2.2rem, 4.5vw, 3.4rem) !important; }
h2 { font-size: clamp(1.7rem, 3.5vw, 2.6rem) !important; }
h3 { font-size: clamp(1.3rem, 2.3vw, 1.8rem) !important; }
p, li, span { font-size: clamp(0.85rem, 1.5vw, 1.05rem) !important; }

/* ============================================
   ENTRANCE ANIMATIONS
   ============================================ */
.animate-in {
    opacity: 0;
    transform: translateY(30px);
    transition: opacity 0.6s ease, transform 0.6s ease;
}

.animate-in.visible {
    opacity: 1;
    transform: translateY(0);
}

.animate-slide-left {
    opacity: 0;
    transform: translateX(-50px);
    transition: opacity 0.7s ease, transform 0.7s ease;
}

.animate-slide-left.visible {
    opacity: 1;
    transform: translateX(0);
}

.animate-slide-right {
    opacity: 0;
    transform: translateX(50px);
    transition: opacity 0.7s ease, transform 0.7s ease;
}

.animate-slide-right.visible {
    opacity: 1;
    transform: translateX(0);
}

.animate-scale {
    opacity: 0;
    transform: scale(0.8);
    transition: opacity 0.6s ease, transform 0.6s ease;
}

.animate-scale.visible {
    opacity: 1;
    transform: scale(1);
}

/* Stagger children animations */
.stagger-children > * {
    opacity: 0;
    transform: translateY(20px);
    transition: opacity 0.5s ease, transform 0.5s ease;
}

.stagger-children.visible > *:nth-child(1) { transition-delay: 0.1s; }
.stagger-children.visible > *:nth-child(2) { transition-delay: 0.2s; }
.stagger-children.visible > *:nth-child(3) { transition-delay: 0.3s; }
.stagger-children.visible > *:nth-child(4) { transition-delay: 0.4s; }
.stagger-children.visible > *:nth-child(5) { transition-delay: 0.5s; }
.stagger-children.visible > *:nth-child(6) { transition-delay: 0.6s; }

.stagger-children.visible > * {
    opacity: 1;
    transform: translateY(0);
}

/* ============================================
   HOVER ENHANCEMENTS
   ============================================ */
.hover-lift {
    transition: transform 0.3s ease, box-shadow 0.3s ease;
}

.hover-lift:hover {
    transform: translateY(-8px);
    box-shadow: 0 15px 40px rgba(76, 175, 80, 0.3) !important;
}

.hover-glow {
    transition: box-shadow 0.3s ease, border-color 0.3s ease;
}

.hover-glow:hover {
    box-shadow: 0 0 30px rgba(76, 175, 80, 0.5) !important;
    border-color: #4CAF50 !important;
}

/* ============================================
   NUMBER COUNTER ANIMATION
   ============================================ */
.counter-number {
    display: inline-block;
    transition: color 0.3s ease;
}

.counter-number.counting {
    color: #4CAF50;
}

/* ============================================
   PULSE ANIMATION FOR ICONS
   ============================================ */
@keyframes pulse-glow {
    0%, 100% {
        box-shadow: 0 0 20px rgba(76, 175, 80, 0.4);
        transform: scale(1);
    }
    50% {
        box-shadow: 0 0 40px rgba(76, 175, 80, 0.8);
        transform: scale(1.05);
    }
}

.pulse-icon {
    animation: pulse-glow 2s ease-in-out infinite;
}

/* ============================================
   ILLUSTRATION STYLING & VISUAL STORYTELLING
   ============================================ */
.story-illustration {
    max-width: 100%;
    height: auto;
    opacity: 0;
    transform: translateY(30px) scale(0.95);
    transition: opacity 0.8s ease, transform 0.8s ease;
    filter: drop-shadow(0 15px 40px rgba(76, 175, 80, 0.25));
}

.story-illustration.visible {
    opacity: 1;
    transform: translateY(0) scale(1);
}

.illustration-container {
    position: relative;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: clamp(1vh, 2vh, 3vh);
}

.illustration-float {
    animation: float 6s ease-in-out infinite;
}

@keyframes float {
    0%, 100% { transform: translateY(0px); }
    50% { transform: translateY(-20px); }
}

/* Illustration-enhanced layouts */
.split-layout {
    display: grid;
    grid-template-columns: 1.3fr 0.7fr;
    gap: clamp(3vw, 5vw, 6vw);
    align-items: center;
}

.split-layout-reverse {
    display: grid;
    grid-template-columns: 0.7fr 1.3fr;
    gap: clamp(3vw, 5vw, 6vw);
    align-items: center;
}

/* SVG illustration enhancements */
.svg-illustration {
    width: 100%;
    height: auto;
    max-width: clamp(200px, 30vw, 400px);
    margin: 0 auto;
    display: block;
}

/* Story cards with large icons (illustration-style) */
.story-card {
    display: flex;
    gap: clamp(1.5vw, 2vw, 2.5vw);
    align-items: start;
    background: rgba(76, 175, 80, 0.06);
    padding: clamp(2vh, 2.5vh, 3vh) clamp(2vw, 2.5vw, 3vw);
    border-radius: 12px;
    border-left: 4px solid #4CAF50;
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
}

.story-card:hover {
    background: rgba(76, 175, 80, 0.12);
    transform: translateX(10px);
    border-left-width: 6px;
    box-shadow: 0 8px 25px rgba(76, 175, 80, 0.2);
}

.story-card-icon {
    font-size: clamp(2.5rem, 4.5vw, 4rem);
    color: #4CAF50;
    min-width: clamp(60px, 9vw, 90px);
    text-align: center;
    line-height: 1;
}

.story-card-content h3 {
    color: #4CAF50;
    margin-bottom: 0.8vh;
    font-size: clamp(1.1rem, 2vw, 1.4rem);
}

.story-card-content p {
    line-height: 1.5;
    color: #ddd;
}

/* Visual background elements */
.visual-story-bg {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    opacity: 0.04;
    z-index: -1 !important;
    pointer-events: none !important;
    background-image: radial-gradient(circle at 20% 30%, rgba(76, 175, 80, 0.15) 0%, transparent 50%),
                      radial-gradient(circle at 80% 70%, rgba(76, 175, 80, 0.15) 0%, transparent 50%);
}

/* Large decorative icons (illustration replacement) */
.mega-icon {
    font-size: clamp(8rem, 15vw, 12rem);
    color: rgba(76, 175, 80, 0.15);
    position: absolute;
    z-index: -1 !important;
    pointer-events: none !important;
    opacity: 0;
    animation: float 8s ease-in-out infinite, fadeInIcon 1s ease 0.5s forwards;
}

@keyframes fadeInIcon {
    from {
        opacity: 0;
        transform: scale(0.5) rotate(-15deg);
    }
    to {
        opacity: 0.6;
        transform: scale(1) rotate(0deg);
    }
}

.mega-icon-top-right {
    top: 5vh;
    right: 5vw;
}

.mega-icon-bottom-left {
    bottom: 5vh;
    left: 5vw;
}

/* Enhanced failure flow animation */
.failure-flow {
    position: relative;
}

.failure-step {
    transition: all 0.4s ease;
}

.failure-step:hover {
    transform: scale(1.05);
}

.failure-arrow {
    opacity: 0;
    animation: fadeInArrow 0.6s ease forwards;
}

@keyframes fadeInArrow {
    from {
        opacity: 0;
        transform: translateX(-20px);
    }
    to {
        opacity: 1;
        transform: translateX(0);
    }
}

/* Problem statement dramatic effects */
.crisis-number-huge {
    animation: countUp 2s ease, pulse 2s ease infinite 2s;
}

@keyframes countUp {
    from {
        opacity: 0;
        transform: scale(0.5);
    }
    to {
        opacity: 1;
        transform: scale(1);
    }
}

/* Story card enhanced animation */
.story-card {
    opacity: 0;
    transform: translateY(30px);
    animation: slideUpFade 0.8s ease forwards;
}

@keyframes slideUpFade {
    to {
        opacity: 1;
        transform: translateY(0);
    }
}

.story-card-icon {
    animation: iconPulse 2s ease infinite;
}

@keyframes iconPulse {
    0%, 100% {
        transform: scale(1);
    }
    50% {
        transform: scale(1.1);
    }
}

/* ============================================
   FULLSCREEN BUTTON
   ============================================ */
.fullscreen-btn {
    position: fixed;
    top: 20px;
    right: 20px;
    z-index: 2000;
    background: rgba(76, 175, 80, 0.15);
    border: 2px solid rgba(76, 175, 80, 0.4);
    color: #4CAF50;
    width: 50px;
    height: 50px;
    border-radius: 12px;
    cursor: pointer;
    font-size: 1.2rem;
    transition: all 0.3s ease;
    display: flex;
    align-items: center;
    justify-content: center;
    backdrop-filter: blur(10px);
}

.fullscreen-btn:hover {
    background: rgba(76, 175, 80, 0.25);
    border-color: #4CAF50;
    transform: scale(1.1);
    box-shadow: 0 8px 25px rgba(76, 175, 80, 0.3);
}

.fullscreen-btn:active {
    transform: scale(0.95);
}

@media (max-width: 768px) {
    .fullscreen-btn {
        top: 10px;
        right: 10px;
        width: 45px;
        height: 45px;
        font-size: 1rem;
    }
}

/* ============================================
   SLIDE CONTAINER & SLIDES
   ============================================ */
.slide-container {
    width: 100vw;
    height: 100vh;
    position: relative;
}

.slide {
    width: 100%;
    height: 100%;
    position: absolute;
    top: 0;
    left: 0;
    display: none;
    padding: 3vh 4vw 12vh 4vw;
    background: #000;
    opacity: 0;
    transition: opacity 0.5s ease;
    overflow-y: auto;
    overflow-x: hidden;
    box-sizing: border-box;
}

.slide.active {
    display: flex;
    flex-direction: column;
    justify-content: center;
    opacity: 1;
}

/* Grid background pattern */
.slide::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-image:
        linear-gradient(rgba(76, 175, 80, 0.02) 1px, transparent 1px),
        linear-gradient(90deg, rgba(76, 175, 80, 0.02) 1px, transparent 1px);
    background-size: 60px 60px;
    z-index: 0;
    pointer-events: none;
}

.slide > * {
    position: relative;
    z-index: 10 !important;
}

/* ============================================
   TITLE SLIDE
   ============================================ */
.title-slide {
    background: radial-gradient(ellipse at 30% 50%, rgba(76, 175, 80, 0.12), transparent 60%),
                radial-gradient(ellipse at 70% 50%, rgba(33, 150, 243, 0.08), transparent 60%),
                #000;
    justify-content: center;
    align-items: center;
    text-align: center;
    padding: 6vh 5vw;
}

.logo-container {
    margin-bottom: 50px;
    animation: fadeInDown 0.8s ease;
}

.logo {
    width: 130px;
    height: 130px;
    margin: 0 auto;
    background: linear-gradient(135deg, #4CAF50, #2E7D32);
    border-radius: 32px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 3.5rem;
    box-shadow: 0 25px 70px rgba(76, 175, 80, 0.35);
    animation: logoFloat 3s ease-in-out infinite;
}

@keyframes logoFloat {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-10px); }
}

.title-slide h1 {
    font-size: clamp(2.5rem, 5.5vw, 5.5rem);
    font-weight: 900;
    margin-bottom: 25px;
    letter-spacing: -2px;
    line-height: 1.15;
    animation: fadeInUp 0.8s ease 0.2s both;
}

.title-slide h1 .highlight {
    background: linear-gradient(90deg, #4CAF50, #8BC34A);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
}

.title-slide .subtitle {
    font-size: clamp(1rem, 2vw, 1.6rem);
    color: #aaa;
    margin-bottom: 60px;
    font-weight: 400;
    animation: fadeInUp 0.8s ease 0.4s both;
}

.title-slide .metrics-bar {
    display: flex;
    gap: 3vw;
    justify-content: center;
    padding: 2.5vh 3vw;
    background: rgba(255, 255, 255, 0.025);
    border: 1px solid rgba(76, 175, 80, 0.25);
    border-radius: 25px;
    backdrop-filter: blur(15px);
    flex-wrap: wrap;
    max-width: 1000px;
    margin: 0 auto;
    animation: fadeInUp 0.8s ease 0.6s both;
}

.metric-item {
    text-align: center;
    min-width: 90px;
}

.metric-item .value {
    font-size: clamp(1.5rem, 3vw, 2.5rem);
    font-weight: 800;
    color: #4CAF50;
    display: block;
}

.metric-item .label {
    font-size: clamp(0.7rem, 1.3vw, 0.9rem);
    color: #888;
    text-transform: uppercase;
    letter-spacing: 1.2px;
    margin-top: 8px;
    font-weight: 500;
}

/* ============================================
   CONTENT SLIDES - HEADINGS
   ============================================ */
h2 {
    font-size: clamp(1.5rem, 3vw, 2.5rem);
    margin-bottom: 2vh;
    font-weight: 800;
    letter-spacing: -1px;
    background: linear-gradient(90deg, #fff, #4CAF50);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    padding-bottom: 1vh;
}

/* Scrollbar styling for slides */
.slide::-webkit-scrollbar {
    width: 8px;
}

.slide::-webkit-scrollbar-track {
    background: rgba(255, 255, 255, 0.05);
    border-radius: 10px;
}

.slide::-webkit-scrollbar-thumb {
    background: rgba(76, 175, 80, 0.5);
    border-radius: 10px;
}

.slide::-webkit-scrollbar-thumb:hover {
    background: rgba(76, 175, 80, 0.7);
}

/* ============================================
   STATS GRID
   ============================================ */
.stats-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: clamp(2vh, 2.5vh, 3vh) clamp(1.5vw, 2vw, 2.5vw);
    margin-top: clamp(2vh, 3vh, 4vh);
    max-height: 75vh;
    width: 100%;
    max-width: 1200px;
    margin-left: auto;
    margin-right: auto;
}

.stat-card {
    background: linear-gradient(135deg, rgba(76, 175, 80, 0.04), rgba(76, 175, 80, 0.01));
    border: 2px solid rgba(76, 175, 80, 0.2);
    border-radius: 18px;
    padding: clamp(2.5vh, 3vh, 4vh) clamp(1.5vw, 2vw, 3vw);
    text-align: center;
    transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
    cursor: pointer;
    position: relative;
    overflow: visible;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    gap: clamp(1vh, 1.5vh, 2vh);
    min-height: clamp(160px, 20vh, 220px);
    height: auto;
}

.stat-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(135deg, rgba(76, 175, 80, 0.15), transparent);
    opacity: 0;
    transition: opacity 0.4s ease;
}

.stat-card:hover {
    transform: translateY(-18px) scale(1.05);
    border-color: #4CAF50;
    box-shadow: 0 25px 70px rgba(76, 175, 80, 0.45);
}

.stat-card:hover::before {
    opacity: 1;
}

.stat-card .icon {
    font-size: clamp(2rem, 4vw, 3.5rem);
    color: #4CAF50;
    flex-shrink: 0;
}

.stat-card .number {
    font-size: clamp(2rem, 4.5vw, 4rem);
    font-weight: 900;
    color: #fff;
    white-space: nowrap;
    overflow: visible;
    flex-shrink: 0;
    line-height: 1.1;
}

.stat-card .label {
    font-size: clamp(0.8rem, 1.4vw, 1.05rem);
    color: #999;
    text-transform: uppercase;
    letter-spacing: 1px;
    font-weight: 600;
    line-height: 1.5;
    white-space: normal;
    word-wrap: break-word;
    width: 100%;
    max-width: 100%;
}

/* ============================================
   PLATFORM CARDS
   ============================================ */
.platform-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: clamp(1.5vh, 2vh, 2.5vh) clamp(1.5vw, 2vw, 2.5vw);
    margin-top: clamp(2vh, 3vh, 4vh);
    max-height: 65vh;
}

.platform-card {
    background: rgba(255, 255, 255, 0.02);
    border: 2px solid rgba(76, 175, 80, 0.2);
    border-radius: 18px;
    padding: 2.5vh 2vw;
    transition: all 0.35s ease;
    cursor: pointer;
    position: relative;
}

.platform-card::before {
    content: '';
    position: absolute;
    left: 0;
    top: 0;
    width: 6px;
    height: 100%;
    background: linear-gradient(180deg, #4CAF50, #2E7D32);
    border-radius: 18px 0 0 18px;
    transform: scaleY(0);
    transition: transform 0.35s ease;
}

.platform-card:hover::before {
    transform: scaleY(1);
}

.platform-card:hover {
    background: rgba(76, 175, 80, 0.06);
    border-color: #4CAF50;
    transform: translateX(12px);
    box-shadow: 0 15px 45px rgba(76, 175, 80, 0.25);
}

.platform-header {
    display: flex;
    align-items: center;
    gap: 1.5vw;
    margin-bottom: 1.5vh;
}

.platform-icon {
    width: clamp(40px, 4.5vw, 50px);
    height: clamp(40px, 4.5vw, 50px);
    background: linear-gradient(135deg, #4CAF50, #2E7D32);
    border-radius: 14px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: clamp(1.1rem, 2vw, 1.5rem);
    flex-shrink: 0;
}

.platform-card h4 {
    color: #fff;
    font-size: clamp(1.1rem, 2vw, 1.4rem);
    font-weight: 700;
}

.platform-card .revenue {
    font-size: clamp(1.4rem, 2.8vw, 2.2rem);
    font-weight: 800;
    color: #4CAF50;
    margin-bottom: 1.5vh;
}

.platform-card .description {
    color: #999;
    font-size: clamp(0.8rem, 1.4vw, 0.95rem);
    line-height: 1.6;
}

/* ============================================
   MODAL/POPUP
   ============================================ */
.modal {
    display: none;
    position: fixed;
    top: 0;
    left: 0;
    width: 100vw;
    height: 100vh;
    background: rgba(0, 0, 0, 0.92);
    backdrop-filter: blur(12px);
    z-index: 10000;
    align-items: center;
    justify-content: center;
    animation: fadeIn 0.3s ease;
    pointer-events: auto;
}

.modal * {
    pointer-events: auto;
}

.modal.show {
    display: flex;
}

.modal-content {
    background: linear-gradient(135deg, rgba(20, 20, 20, 0.97), rgba(30, 30, 30, 0.97));
    border: 2px solid rgba(76, 175, 80, 0.3);
    border-radius: clamp(16px, 3vw, 32px);
    padding: clamp(3vh, 5vh, 6vh) clamp(2.5vw, 4vw, 5vw);
    max-width: min(90vw, 1000px);
    width: 92vw;
    max-height: 88vh;
    overflow-y: auto;
    position: relative;
    box-shadow: 0 3vh 10vh rgba(0, 0, 0, 0.85);
    animation: slideUp 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
    pointer-events: auto;
    z-index: 10001;
}

.modal-content::-webkit-scrollbar {
    width: 8px;
}

.modal-content::-webkit-scrollbar-track {
    background: rgba(255, 255, 255, 0.05);
    border-radius: 10px;
}

.modal-content::-webkit-scrollbar-thumb {
    background: rgba(76, 175, 80, 0.5);
    border-radius: 10px;
}

.modal-content::-webkit-scrollbar-thumb:hover {
    background: rgba(76, 175, 80, 0.7);
}

@media (max-width: 768px) {
    .modal-content {
        padding: 3vh 4vw;
        width: 96vw;
        max-height: 92vh;
        border-radius: clamp(12px, 2vw, 20px);
    }
}

@keyframes slideUp {
    from {
        transform: translateY(120px);
        opacity: 0;
    }
    to {
        transform: translateY(0);
        opacity: 1;
    }
}

.modal-close {
    position: absolute;
    top: 22px;
    right: 22px;
    background: rgba(255, 255, 255, 0.08);
    border: none;
    color: #fff;
    font-size: 2.2rem;
    cursor: pointer;
    width: 55px;
    height: 55px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: all 0.3s ease;
    font-family: Arial, sans-serif;
}

.modal-close:hover {
    background: #4CAF50;
    transform: rotate(90deg);
}

.modal h3 {
    font-size: clamp(1.6rem, 3.5vw, 2.8rem);
    margin-bottom: clamp(1.5vh, 2.5vh, 3vh);
    color: #4CAF50;
    font-weight: 800;
    line-height: 1.3;
}

.modal p {
    font-size: clamp(1rem, 2vw, 1.15rem);
    line-height: 1.75;
    color: #ccc;
    margin-bottom: clamp(1.5vh, 2vh, 2.5vh);
}

.modal-stats {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: clamp(2vh, 2.5vh, 3vh) clamp(2vw, 2.5vw, 3vw);
    margin-top: clamp(2vh, 3vh, 4vh);
    width: 100%;
}

@media (max-width: 768px) {
    .modal-stats {
        grid-template-columns: 1fr;
        gap: 1.5vh;
    }
}

.modal-stat {
    background: rgba(76, 175, 80, 0.08);
    padding: clamp(2.5vh, 3vh, 4vh) clamp(2vw, 2.5vw, 3vw);
    border-radius: clamp(12px, 1.5vw, 18px);
    border: 1px solid rgba(76, 175, 80, 0.25);
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    position: relative;
    overflow: visible;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    text-align: center;
    min-height: clamp(100px, 12vh, 140px);
}

.modal-stat:hover {
    background: rgba(76, 175, 80, 0.12);
    border-color: rgba(76, 175, 80, 0.4);
}

.modal-stat-value {
    font-size: clamp(2rem, 4vw, 3rem);
    font-weight: 900;
    color: #4CAF50;
    margin-bottom: clamp(0.5vh, 1vh, 1.5vh);
    line-height: 1.1;
}

.modal-stat-label {
    font-size: clamp(0.9rem, 1.8vw, 1.1rem);
    color: #bbb;
    line-height: 1.5;
    font-weight: 600;
}

/* ============================================
   CHART CONTAINER
   ============================================ */
.chart-container {
    background: rgba(255, 255, 255, 0.02);
    border: 2px solid rgba(76, 175, 80, 0.2);
    border-radius: 25px;
    padding: clamp(2vh, 3vh, 4vh) clamp(2vw, 3vw, 4vw);
    margin-top: clamp(2vh, 3vh, 4vh);
    position: relative;
    height: clamp(45vh, 50vh, 55vh);
    max-height: 55vh;
}

.chart-bars {
    display: flex;
    align-items: flex-end;
    justify-content: space-around;
    height: 100%;
    padding-top: 70px;
}

.bar-group {
    text-align: center;
    flex: 1;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: flex-end;
    cursor: pointer;
    transition: all 0.3s ease;
}

.bar-group:hover .bar {
    filter: brightness(1.35);
    transform: scaleY(1.05);
}

.bar {
    width: clamp(55px, 11vw, 90px);
    background: linear-gradient(180deg, #4CAF50, #2E7D32);
    border-radius: 12px 12px 0 0;
    position: relative;
    transition: all 0.5s cubic-bezier(0.175, 0.885, 0.32, 1.275);
    box-shadow: 0 -12px 35px rgba(76, 175, 80, 0.35);
}

@keyframes growBar {
    from { height: 0; opacity: 0; }
    to { opacity: 1; }
}

.bar-label {
    margin-top: 1.5vh;
    color: #999;
    font-weight: 700;
    font-size: clamp(0.7rem, 1.3vw, 0.9rem);
    text-transform: uppercase;
    letter-spacing: 1.2px;
}

.bar-value {
    position: absolute;
    top: -4vh;
    left: 50%;
    transform: translateX(-50%);
    color: #4CAF50;
    font-weight: 800;
    font-size: clamp(0.85rem, 1.6vw, 1.2rem);
    white-space: nowrap;
}

/* ============================================
   NAVIGATION
   ============================================ */
.navigation {
    position: fixed;
    bottom: 25px;
    right: 25px;
    display: flex;
    gap: 12px;
    z-index: 9000;
}

@media (max-width: 768px) {
    .navigation {
        bottom: 12px;
        right: 12px;
        gap: 8px;
    }
}

.nav-btn {
    background: rgba(76, 175, 80, 0.08);
    border: 2px solid rgba(76, 175, 80, 0.35);
    color: #fff;
    padding: 14px 28px;
    font-size: clamp(0.85rem, 1.6vw, 1.05rem);
    border-radius: 50px;
    cursor: pointer;
    transition: all 0.3s ease;
    font-weight: 700;
    font-family: 'Poppins', sans-serif;
    text-transform: uppercase;
    letter-spacing: 1.2px;
}

@media (max-width: 768px) {
    .nav-btn {
        padding: 12px 22px;
        font-size: 0.85rem;
    }
}

.nav-btn:hover:not(:disabled) {
    background: #4CAF50;
    border-color: #4CAF50;
    transform: translateY(-4px);
    box-shadow: 0 12px 35px rgba(76, 175, 80, 0.45);
}

.nav-btn:active:not(:disabled) {
    transform: translateY(-2px);
}

.nav-btn:disabled {
    opacity: 0.18;
    cursor: not-allowed;
}

/* ============================================
   SLIDE COUNTER
   ============================================ */
.slide-counter {
    position: fixed;
    bottom: 25px;
    left: 25px;
    color: #4CAF50;
    font-size: clamp(1.05rem, 2.2vw, 1.4rem);
    font-weight: 700;
    background: rgba(76, 175, 80, 0.08);
    padding: 14px 28px;
    border-radius: 50px;
    border: 2px solid rgba(76, 175, 80, 0.35);
}

@media (max-width: 768px) {
    .slide-counter {
        bottom: 12px;
        left: 12px;
        padding: 12px 22px;
        font-size: 0.95rem;
    }
}

/* ============================================
   CLICK INDICATOR
   ============================================ */
.click-indicator {
    position: absolute;
    bottom: 2vh;
    right: 2vw;
    width: 11px;
    height: 11px;
    background: #4CAF50;
    border-radius: 50%;
    box-shadow: 0 0 12px #4CAF50, 0 0 24px #4CAF50, 0 0 36px #4CAF50;
    animation: pulse 2s infinite;
}

@keyframes pulse {
    0%, 100% {
        opacity: 1;
        transform: scale(1);
    }
    50% {
        opacity: 0.4;
        transform: scale(1.3);
    }
}

/* ============================================
   ANIMATIONS
   ============================================ */
@keyframes fadeIn {
    from { opacity: 0; }
    to { opacity: 1; }
}

@keyframes fadeInDown {
    from {
        opacity: 0;
        transform: translateY(-30px);
    }
    to {
        opacity: 1;
        transform: translateY(0);
    }
}

@keyframes fadeInUp {
    from {
        opacity: 0;
        transform: translateY(30px);
    }
    to {
        opacity: 1;
        transform: translateY(0);
    }
}

/* ============================================
   LOGO IMAGE (ACTUAL UAEI LOGO)
   ============================================ */
.logo-img {
    max-width: 180px;
    max-height: 180px;
    filter: drop-shadow(0 20px 50px rgba(76, 175, 80, 0.4));
    animation: logoFloat 3s ease-in-out infinite;
}

.final-logo {
    max-width: 280px;
    width: 100%;
    filter: drop-shadow(0 10px 30px rgba(76, 175, 80, 0.4));
}

.metric-arrow {
    font-size: clamp(1.5rem, 3vw, 2.5rem);
    color: #4CAF50;
    font-weight: 300;
}

/* ============================================
   CRISIS SLIDE
   ============================================ */
.crisis-container {
    max-width: 1200px;
    margin: 0 auto;
}

.crisis-stat {
    text-align: center;
    margin-bottom: 3vh;
}

.crisis-number {
    font-size: clamp(4rem, 8vw, 7rem);
    font-weight: 900;
    color: #ff5252;
    line-height: 1;
}

.crisis-label {
    font-size: clamp(1rem, 2vw, 1.3rem);
    color: #999;
    margin-top: 1vh;
}

.two-column-layout {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 3vw;
    margin: 3vh 0;
}

@media (max-width: 768px) {
    .two-column-layout {
        grid-template-columns: 1fr;
    }
}

.info-box {
    padding: 3vh 2vw;
    border-radius: 18px;
    border: 2px solid;
}

.info-box.danger {
    background: rgba(255, 82, 82, 0.05);
    border-color: rgba(255, 82, 82, 0.3);
}

.info-box.warning {
    background: rgba(255, 193, 7, 0.05);
    border-color: rgba(255, 193, 7, 0.3);
}

.info-box h3 {
    font-size: clamp(1.2rem, 2.2vw, 1.5rem);
    margin-bottom: 2vh;
    display: flex;
    align-items: center;
    gap: 10px;
}

.info-box.danger h3 {
    color: #ff5252;
}

.info-box.warning h3 {
    color: #FFC107;
}

.large-list {
    list-style: none;
    font-size: clamp(0.95rem, 1.8vw, 1.15rem);
    line-height: 1.8;
}

.large-list li {
    margin-bottom: 1vh;
    padding-left: 1.5em;
    position: relative;
}

.large-list li::before {
    content: '•';
    position: absolute;
    left: 0.5em;
    color: #4CAF50;
    font-weight: bold;
}

.solution-callout {
    text-align: center;
    padding: 2.5vh 3vw;
    background: rgba(76, 175, 80, 0.1);
    border: 2px solid #4CAF50;
    border-radius: 20px;
    margin-top: 3vh;
    font-size: clamp(1.1rem, 2.2vw, 1.5rem);
    font-weight: 700;
    color: #4CAF50;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 15px;
}

/* ============================================
   BREAKTHROUGH SLIDE
   ============================================ */
.breakthrough-container {
    max-width: 900px;
    margin: 0 auto;
}

.innovation-box {
    text-align: center;
    padding: 2vh 2vw;
    background: rgba(76, 175, 80, 0.1);
    border: 2px solid #4CAF50;
    border-radius: 15px;
    margin-bottom: 2vh;
}

.innovation-box h3 {
    font-size: clamp(1.4rem, 2.8vw, 2rem);
    color: #4CAF50;
    margin-bottom: 0.5vh;
}

.innovation-subtitle {
    font-size: clamp(0.9rem, 1.7vw, 1.1rem);
    color: #ccc;
}

.process-flow {
    display: flex;
    flex-direction: row;
    align-items: center;
    gap: 1vw;
    margin: 2vh 0;
    justify-content: center;
}

.process-step {
    padding: 1.5vh 1.5vw;
    background: rgba(255, 255, 255, 0.03);
    border: 2px solid rgba(76, 175, 80, 0.3);
    border-radius: 12px;
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    gap: 1vh;
    transition: all 0.3s ease;
    flex: 1;
    max-width: 200px;
}

.process-step:hover {
    background: rgba(76, 175, 80, 0.05);
    border-color: #4CAF50;
    transform: translateY(-5px);
}

.process-step.success {
    background: rgba(76, 175, 80, 0.1);
    border-color: #4CAF50;
}

.step-number {
    width: 35px;
    height: 35px;
    background: linear-gradient(135deg, #4CAF50, #2E7D32);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1rem;
    font-weight: 900;
    flex-shrink: 0;
}

.step-content {
    font-size: clamp(0.8rem, 1.5vw, 0.95rem);
    line-height: 1.4;
}

.flow-arrow {
    text-align: center;
    font-size: 1.5rem;
    color: #4CAF50;
    font-weight: 300;
    flex-shrink: 0;
}

.genius-box {
    padding: 1.5vh 2vw;
    background: rgba(255, 193, 7, 0.08);
    border: 2px solid rgba(255, 193, 7, 0.4);
    border-radius: 12px;
    margin-top: 2vh;
}

.genius-box h4 {
    font-size: clamp(1rem, 2vw, 1.3rem);
    color: #FFC107;
    margin-bottom: 0.5vh;
    display: flex;
    align-items: center;
    gap: 10px;
}

.genius-box p {
    font-size: clamp(0.85rem, 1.6vw, 1rem);
    color: #ccc;
    line-height: 1.5;
}

/* ============================================
   PACKAGE SLIDE
   ============================================ */
.package-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 3vw;
    margin-top: 3vh;
}

@media (max-width: 768px) {
    .package-grid {
        grid-template-columns: 1fr;
    }
}

.package-column h3 {
    font-size: clamp(1.3rem, 2.5vw, 1.7rem);
    margin-bottom: 2vh;
    display: flex;
    align-items: center;
    gap: 10px;
}

.component-list, .benefit-list {
    display: flex;
    flex-direction: column;
    gap: 1.5vh;
}

.component-item {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 1.5vh 2vw;
    background: rgba(76, 175, 80, 0.05);
    border: 1px solid rgba(76, 175, 80, 0.2);
    border-radius: 12px;
    transition: all 0.3s ease;
}

.component-item:hover {
    background: rgba(76, 175, 80, 0.1);
    border-color: #4CAF50;
    transform: translateX(5px);
}

.component-item .amount {
    font-size: clamp(1.3rem, 2.5vw, 1.8rem);
    font-weight: 800;
    color: #4CAF50;
}

.component-item .desc {
    font-size: clamp(0.95rem, 1.8vw, 1.15rem);
    color: #ccc;
}

.benefit-item {
    display: flex;
    align-items: center;
    gap: 15px;
    padding: 1.5vh 2vw;
    background: rgba(255, 255, 255, 0.02);
    border: 1px solid rgba(76, 175, 80, 0.2);
    border-radius: 12px;
    font-size: clamp(0.95rem, 1.8vw, 1.15rem);
    transition: all 0.3s ease;
}

.benefit-item:hover {
    background: rgba(76, 175, 80, 0.05);
    border-color: #4CAF50;
}

.benefit-item i {
    color: #4CAF50;
    font-size: 1.3em;
}

.repayment-box {
    margin-top: 3vh;
    padding: 2vh 3vw;
    background: rgba(76, 175, 80, 0.1);
    border: 2px solid #4CAF50;
    border-radius: 15px;
    text-align: center;
    font-size: clamp(1.1rem, 2.2vw, 1.4rem);
    font-weight: 700;
    color: #4CAF50;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 15px;
}

/* ============================================
   OPPORTUNITY / STATS INLINE
   ============================================ */
.opportunity-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 3vw;
    margin-top: 3vh;
}

@media (max-width: 768px) {
    .opportunity-grid {
        grid-template-columns: 1fr;
    }
}

.opportunity-section {
    padding: 3vh 2vw;
    background: rgba(255, 255, 255, 0.02);
    border: 2px solid rgba(76, 175, 80, 0.3);
    border-radius: 18px;
}

.opportunity-section h3 {
    font-size: clamp(1.3rem, 2.5vw, 1.7rem);
    margin-bottom: 2.5vh;
    display: flex;
    align-items: center;
    gap: 10px;
}

.stats-list {
    display: flex;
    flex-direction: column;
    gap: 1.8vh;
}

.stat-item-inline {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 1.2vh 0;
    border-bottom: 1px solid rgba(76, 175, 80, 0.15);
}

.stat-item-inline .label {
    font-size: clamp(0.95rem, 1.8vw, 1.15rem);
    color: #999;
}

.stat-item-inline .value {
    font-size: clamp(1.1rem, 2.2vw, 1.4rem);
    font-weight: 800;
    color: #4CAF50;
}

.mandate-box, .derisked-box, .bee-box, .moats-footer,
.firstmover-box, .risk-footer, .mandate-final, .return-box {
    margin-top: clamp(2vh, 2.5vh, 3vh);
    padding: clamp(1.5vh, 2vh, 2.5vh) clamp(2vw, 3vw, 4vw);
    background: rgba(76, 175, 80, 0.1);
    border: 2px solid #4CAF50;
    border-radius: 15px;
    text-align: center;
    font-size: clamp(0.95rem, 1.8vw, 1.2rem);
    font-weight: 700;
    color: #4CAF50;
    line-height: 1.4;
}

.chart-note {
    text-align: center;
    margin-top: 2vh;
    color: #999;
    font-size: clamp(0.9rem, 1.6vw, 1.05rem);
    font-style: italic;
}

/* ============================================
   REVENUE STREAMS GRID
   ============================================ */
.streams-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 2.5vh 2vw;
    margin-top: 3vh;
}

.stream-card {
    padding: 2.5vh 2vw;
    background: rgba(255, 255, 255, 0.02);
    border: 2px solid rgba(76, 175, 80, 0.2);
    border-radius: 18px;
    cursor: pointer;
    transition: all 0.3s ease;
}

.stream-card:hover {
    background: rgba(76, 175, 80, 0.08);
    border-color: #4CAF50;
    transform: translateY(-8px);
    box-shadow: 0 15px 45px rgba(76, 175, 80, 0.3);
}

.stream-header {
    display: flex;
    align-items: center;
    gap: 12px;
    margin-bottom: 2vh;
}

.stream-header i {
    font-size: clamp(1.5rem, 3vw, 2rem);
    color: #4CAF50;
}

.stream-header h4 {
    font-size: clamp(1.1rem, 2vw, 1.4rem);
    color: #fff;
}

.stream-percentage {
    font-size: clamp(2rem, 4vw, 3rem);
    font-weight: 900;
    color: #4CAF50;
    margin-bottom: 0.5vh;
}

.stream-amount {
    font-size: clamp(1rem, 2vw, 1.3rem);
    color: #ccc;
    margin-bottom: 1.5vh;
    font-weight: 600;
}

.stream-card p {
    font-size: clamp(0.9rem, 1.6vw, 1.05rem);
    color: #999;
    line-height: 1.5;
}

/* ============================================
   READY / CHECK LISTS
   ============================================ */
.ready-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 3vw;
    margin-top: 3vh;
}

@media (max-width: 768px) {
    .ready-grid {
        grid-template-columns: 1fr;
    }
}

.ready-section {
    padding: 3vh 2vw;
    background: rgba(255, 255, 255, 0.02);
    border: 2px solid rgba(76, 175, 80, 0.3);
    border-radius: 18px;
}

.ready-section h3 {
    font-size: clamp(1.3rem, 2.5vw, 1.7rem);
    margin-bottom: 2vh;
    display: flex;
    align-items: center;
    gap: 10px;
}

.check-list {
    list-style: none;
    display: flex;
    flex-direction: column;
    gap: 1.5vh;
}

.check-list li {
    font-size: clamp(1rem, 2vw, 1.25rem);
    display: flex;
    align-items: center;
    gap: 12px;
    padding: 1vh 0;
}

.check-list i {
    color: #4CAF50;
    font-size: 1.2em;
}

/* ============================================
   BUDGET BREAKDOWN
   ============================================ */
.budget-container {
    margin-top: 3vh;
}

.budget-split {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 3vw;
}

@media (max-width: 768px) {
    .budget-split {
        grid-template-columns: 1fr;
    }
}

.budget-column h3 {
    font-size: clamp(1.4rem, 2.8vw, 1.9rem);
    margin-bottom: 2.5vh;
    color: #4CAF50;
}

.budget-item {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 1.5vh 2vw;
    background: rgba(76, 175, 80, 0.05);
    border: 1px solid rgba(76, 175, 80, 0.2);
    border-radius: 12px;
    margin-bottom: 1.5vh;
}

.budget-label {
    font-size: clamp(1rem, 2vw, 1.25rem);
    color: #ccc;
}

.budget-value {
    font-size: clamp(1.2rem, 2.4vw, 1.6rem);
    font-weight: 800;
    color: #4CAF50;
}

.budget-subitem {
    font-size: clamp(0.85rem, 1.6vw, 1rem);
    color: #777;
    margin: -1vh 0 1.5vh 2vw;
    font-style: italic;
}

.funding-box {
    padding: 2.5vh 2vw;
    border-radius: 15px;
    margin-bottom: 2vh;
}

.funding-box.idc {
    background: rgba(33, 150, 243, 0.08);
    border: 2px solid rgba(33, 150, 243, 0.3);
}

.funding-box.grants {
    background: rgba(76, 175, 80, 0.08);
    border: 2px solid rgba(76, 175, 80, 0.3);
}

.funding-box.equity {
    background: rgba(255, 193, 7, 0.08);
    border: 2px solid rgba(255, 193, 7, 0.3);
}

.funding-header {
    font-size: clamp(1.1rem, 2.2vw, 1.4rem);
    font-weight: 700;
    margin-bottom: 1vh;
}

.funding-box.idc .funding-header {
    color: #2196F3;
}

.funding-box.grants .funding-header {
    color: #4CAF50;
}

.funding-box.equity .funding-header {
    color: #FFC107;
}

.funding-amount {
    font-size: clamp(1.8rem, 3.5vw, 2.5rem);
    font-weight: 900;
    margin-bottom: 1vh;
}

.funding-box.idc .funding-amount {
    color: #2196F3;
}

.funding-box.grants .funding-amount {
    color: #4CAF50;
}

.funding-box.equity .funding-amount {
    color: #FFC107;
}

.funding-details {
    font-size: clamp(0.9rem, 1.7vw, 1.05rem);
    color: #999;
    line-height: 1.6;
}

/* ============================================
   TIMELINE TABLE
   ============================================ */
.timeline-container {
    margin-top: 3vh;
    border: 2px solid rgba(76, 175, 80, 0.3);
    border-radius: 15px;
    overflow: hidden;
}

.timeline-row {
    display: grid;
    grid-template-columns: 80px 1fr 1.2fr 1.2fr 1.2fr;
    padding: 1.5vh 2vw;
    border-bottom: 1px solid rgba(76, 175, 80, 0.2);
    transition: all 0.3s ease;
    cursor: pointer;
}

@media (max-width: 768px) {
    .timeline-row {
        grid-template-columns: 60px repeat(4, 1fr);
        font-size: 0.85rem;
    }
}

.timeline-row:hover:not(.header) {
    background: rgba(76, 175, 80, 0.05);
}

.timeline-row.header {
    background: rgba(76, 175, 80, 0.15);
    font-weight: 700;
    color: #4CAF50;
    cursor: default;
}

.timeline-row > div {
    padding: 0 10px;
    display: flex;
    align-items: center;
}

.timeline-row .year {
    font-size: clamp(1.2rem, 2.4vw, 1.6rem);
    font-weight: 900;
    color: #4CAF50;
}

.timeline-row .positive {
    color: #4CAF50;
    font-weight: 700;
}

.timeline-row .negative {
    color: #ff5252;
    font-weight: 700;
}

.year1-focus {
    margin-top: 3vh;
}

.year1-focus h3 {
    font-size: clamp(1.3rem, 2.5vw, 1.7rem);
    margin-bottom: 2vh;
    color: #4CAF50;
}

.quarters-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
    gap: 1.5vh 1.5vw;
}

.quarter-box {
    padding: 2vh 1.5vw;
    background: rgba(76, 175, 80, 0.05);
    border: 1px solid rgba(76, 175, 80, 0.2);
    border-radius: 12px;
    font-size: clamp(0.9rem, 1.7vw, 1.05rem);
    line-height: 1.6;
}

/* ============================================
   IMPACT METRICS
   ============================================ */
.impact-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 3vw;
    margin-top: 3vh;
}

@media (max-width: 768px) {
    .impact-grid {
        grid-template-columns: 1fr;
    }
}

.impact-section {
    padding: 3vh 2vw;
    border-radius: 18px;
}

.impact-section.economic {
    background: rgba(33, 150, 243, 0.05);
    border: 2px solid rgba(33, 150, 243, 0.3);
}

.impact-section.environmental {
    background: rgba(76, 175, 80, 0.05);
    border: 2px solid rgba(76, 175, 80, 0.3);
}

.impact-section h3 {
    font-size: clamp(1.3rem, 2.5vw, 1.7rem);
    margin-bottom: 2.5vh;
    display: flex;
    align-items: center;
    gap: 10px;
}

.impact-section.economic h3 {
    color: #2196F3;
}

.impact-section.environmental h3 {
    color: #4CAF50;
}

.impact-items {
    display: flex;
    flex-direction: column;
    gap: 2vh;
}

.impact-stat {
    display: flex;
    flex-direction: column;
    gap: 0.5vh;
}

.impact-stat .stat-label {
    font-size: clamp(0.9rem, 1.7vw, 1.05rem);
    color: #999;
}

.impact-stat .stat-value {
    font-size: clamp(1.3rem, 2.5vw, 1.8rem);
    font-weight: 800;
    color: #fff;
}

.impact-stat .stat-change {
    display: inline-block;
    margin-left: 10px;
    padding: 4px 12px;
    background: rgba(76, 175, 80, 0.2);
    border-radius: 20px;
    font-size: 0.9em;
    color: #4CAF50;
}

/* ============================================
   MOATS CARDS
   ============================================ */
.moats-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 2.5vh 2vw;
    margin-top: 3vh;
}

.moat-card {
    padding: 2.5vh 2vw;
    background: rgba(76, 175, 80, 0.05);
    border: 2px solid rgba(76, 175, 80, 0.3);
    border-radius: 18px;
    text-align: center;
    transition: all 0.3s ease;
}

.moat-card:hover {
    background: rgba(76, 175, 80, 0.1);
    border-color: #4CAF50;
    transform: translateY(-10px);
    box-shadow: 0 20px 50px rgba(76, 175, 80, 0.3);
}

.moat-number {
    width: 50px;
    height: 50px;
    background: linear-gradient(135deg, #4CAF50, #2E7D32);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.5rem;
    font-weight: 900;
    margin: 0 auto 1.5vh;
}

.moat-card h4 {
    font-size: clamp(1.1rem, 2.2vw, 1.4rem);
    margin-bottom: 1.5vh;
    color: #fff;
}

.moat-stat {
    font-size: clamp(1.3rem, 2.5vw, 1.8rem);
    font-weight: 800;
    color: #4CAF50;
    margin-bottom: 1vh;
}

.moat-desc {
    font-size: clamp(0.9rem, 1.7vw, 1.05rem);
    color: #999;
}

/* ============================================
   MARKET OPPORTUNITY
   ============================================ */
.market-split {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 3vw;
    margin-top: 3vh;
}

@media (max-width: 768px) {
    .market-split {
        grid-template-columns: 1fr;
    }
}

.market-section {
    padding: 3vh 2vw;
    background: rgba(255, 255, 255, 0.02);
    border: 2px solid rgba(76, 175, 80, 0.3);
    border-radius: 18px;
}

.market-section h3 {
    font-size: clamp(1.3rem, 2.5vw, 1.7rem);
    margin-bottom: 2.5vh;
    display: flex;
    align-items: center;
    gap: 10px;
    color: #4CAF50;
}

.market-stats {
    display: flex;
    flex-direction: column;
    gap: 2vh;
}

.market-item {
    display: flex;
    flex-direction: column;
    gap: 0.5vh;
}

.market-number {
    font-size: clamp(1.5rem, 3vw, 2.2rem);
    font-weight: 900;
    color: #4CAF50;
}

.market-label {
    font-size: clamp(0.95rem, 1.8vw, 1.15rem);
    color: #999;
}

.market-highlight {
    margin-top: 1vh;
    padding: 1.5vh 2vw;
    background: rgba(76, 175, 80, 0.1);
    border: 1px solid #4CAF50;
    border-radius: 12px;
    text-align: center;
    font-weight: 700;
    color: #4CAF50;
}

/* ============================================
   DE-RISK SECTIONS
   ============================================ */
.derisk-container {
    margin-top: 3vh;
}

.stress-test-box {
    padding: 3vh 3vw;
    background: rgba(76, 175, 80, 0.08);
    border: 2px solid #4CAF50;
    border-radius: 18px;
    text-align: center;
    margin-bottom: 3vh;
}

.stress-test-box h3 {
    font-size: clamp(1.4rem, 2.8vw, 1.9rem);
    color: #4CAF50;
    margin-bottom: 1.5vh;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 12px;
}

.stress-main {
    font-size: clamp(1.3rem, 2.5vw, 1.8rem);
    font-weight: 800;
    color: #fff;
    margin-bottom: 1vh;
}

.stress-detail {
    font-size: clamp(0.95rem, 1.8vw, 1.15rem);
    color: #999;
    line-height: 1.6;
}

.derisk-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 2.5vh 2vw;
}

.derisk-section {
    padding: 2.5vh 2vw;
    background: rgba(255, 255, 255, 0.02);
    border: 2px solid rgba(76, 175, 80, 0.2);
    border-radius: 15px;
}

.derisk-section h4 {
    font-size: clamp(1.2rem, 2.4vw, 1.6rem);
    color: #4CAF50;
    margin-bottom: 1.5vh;
}

.derisk-list {
    list-style: none;
    display: flex;
    flex-direction: column;
    gap: 1.2vh;
}

.derisk-list li {
    font-size: clamp(0.95rem, 1.8vw, 1.15rem);
    color: #ccc;
    padding-left: 1.5em;
    position: relative;
    line-height: 1.5;
}

.derisk-list li::before {
    content: '✓';
    position: absolute;
    left: 0;
    color: #4CAF50;
    font-weight: bold;
}

/* ============================================
   THE ASK SLIDE
   ============================================ */
.ask-container {
    max-width: 1100px;
    margin: 0 auto;
}

.ask-amount-box {
    text-align: center;
    padding: 3vh 3vw;
    background: rgba(76, 175, 80, 0.1);
    border: 3px solid #4CAF50;
    border-radius: 20px;
    margin-bottom: 3vh;
}

.ask-amount {
    font-size: clamp(3rem, 6vw, 5rem);
    font-weight: 900;
    color: #4CAF50;
    line-height: 1;
    margin-bottom: 1vh;
}

.ask-structure {
    font-size: clamp(1.1rem, 2.2vw, 1.5rem);
    color: #ccc;
    margin-bottom: 0.5vh;
}

.ask-dilution {
    font-size: clamp(1rem, 2vw, 1.3rem);
    color: #999;
    font-weight: 600;
}

.ask-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 3vw;
}

@media (max-width: 768px) {
    .ask-grid {
        grid-template-columns: 1fr;
    }
}

.ask-section {
    padding: 3vh 2vw;
    border-radius: 18px;
}

.ask-section.returns {
    background: rgba(33, 150, 243, 0.05);
    border: 2px solid rgba(33, 150, 243, 0.3);
}

.ask-section.ready {
    background: rgba(76, 175, 80, 0.05);
    border: 2px solid rgba(76, 175, 80, 0.3);
}

.ask-section h3 {
    font-size: clamp(1.4rem, 2.8vw, 1.9rem);
    margin-bottom: 2.5vh;
}

.ask-section.returns h3 {
    color: #2196F3;
}

.ask-section.ready h3 {
    color: #4CAF50;
}

.ask-items {
    display: flex;
    flex-direction: column;
    gap: 1.8vh;
}

.ask-item {
    display: flex;
    align-items: center;
    gap: 15px;
    font-size: clamp(1.1rem, 2.2vw, 1.4rem);
    font-weight: 600;
    padding: 1.2vh 0;
}

.ask-item i {
    font-size: 1.4em;
    width: 35px;
    text-align: center;
}

.ask-section.returns .ask-item i {
    color: #2196F3;
}

.ask-section.ready .ask-item i {
    color: #4CAF50;
}

/* ============================================
   THESIS FLOW
   ============================================ */
.thesis-container {
    max-width: 100%;
    margin: 1vh auto 0;
}

.thesis-flow {
    display: flex;
    flex-direction: row;
    align-items: center;
    justify-content: center;
    gap: 1vw;
    flex-wrap: nowrap;
}

.thesis-step {
    padding: 1.5vh 1.5vw;
    background: rgba(255, 255, 255, 0.02);
    border: 2px solid rgba(76, 175, 80, 0.3);
    border-radius: 12px;
    text-align: center;
    flex: 1;
    max-width: 150px;
    transition: all 0.3s ease;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
}

.thesis-step:hover {
    background: rgba(76, 175, 80, 0.05);
    border-color: #4CAF50;
    transform: translateY(-5px);
}

.thesis-step.success {
    background: rgba(76, 175, 80, 0.1);
    border-color: #4CAF50;
}

.thesis-icon {
    font-size: clamp(1.8rem, 3.5vw, 2.5rem);
    color: #4CAF50;
    margin-bottom: 0.75vh;
}

.thesis-step h3 {
    font-size: clamp(1.1rem, 2vw, 1.4rem);
    color: #fff;
    margin-bottom: 0.25vh;
}

.thesis-step p {
    font-size: clamp(0.75rem, 1.4vw, 0.9rem);
    color: #999;
}

.thesis-arrow {
    font-size: 2rem;
    color: #4CAF50;
    font-weight: 300;
    flex-shrink: 0;
}

.thesis-result {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 2vw;
    margin-top: 2vh;
    padding: 1.5vh 2vw;
    background: rgba(76, 175, 80, 0.08);
    border: 2px solid #4CAF50;
    border-radius: 15px;
}

.result-stat {
    font-size: clamp(1.4rem, 2.8vw, 2rem);
    font-weight: 900;
    color: #4CAF50;
}

.result-stat.competitor {
    color: #ff5252;
}

.result-vs {
    font-size: clamp(1.2rem, 2.2vw, 1.6rem);
    color: #999;
    font-weight: 300;
}

/* ============================================
   FINAL SLIDE / CONTACT
   ============================================ */
.final-slide {
    background: radial-gradient(ellipse at center, rgba(76, 175, 80, 0.1), transparent 70%),
                #000;
    display: flex;
    justify-content: center;
    align-items: center;
}

.final-layout {
    display: flex;
    flex-direction: row;
    align-items: center;
    gap: 5vw;
    width: 100%;
    max-width: 1400px;
    padding: 2vh 3vw;
}

.final-left {
    flex: 1;
    text-align: left;
}

.final-right {
    flex-shrink: 0;
    display: flex;
    align-items: center;
    justify-content: center;
}

.final-slide h1 {
    font-size: clamp(2.5rem, 5vw, 4rem);
    font-weight: 900;
    margin-bottom: 0.5vh;
    color: #4CAF50;
}

.final-subtitle {
    font-size: clamp(1rem, 2vw, 1.5rem);
    color: #999;
    margin-bottom: 2vh;
}

.ubuntu-quote {
    padding: 2vh 3vw;
    background: rgba(76, 175, 80, 0.05);
    border-left: 4px solid #4CAF50;
    border-radius: 10px;
    margin: 2vh 0;
    position: relative;
}

.ubuntu-quote i {
    color: #4CAF50;
    opacity: 0.3;
    font-size: 1.2rem;
}

.ubuntu-quote p {
    font-size: clamp(0.95rem, 1.8vw, 1.2rem);
    font-style: italic;
    color: #ccc;
    line-height: 1.5;
    margin: 0.5vh 0;
}

.contact-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 2vh 2vw;
    margin-top: 2vh;
    text-align: left;
}

.contact-section {
    padding: 1.5vh 1.5vw;
    background: rgba(255, 255, 255, 0.02);
    border: 1px solid rgba(76, 175, 80, 0.2);
    border-radius: 12px;
}

.contact-section h3 {
    font-size: clamp(1rem, 1.9vw, 1.3rem);
    color: #4CAF50;
    margin-bottom: 0.5vh;
}

.contact-section p {
    font-size: clamp(0.8rem, 1.5vw, 0.95rem);
    color: #ccc;
    margin-bottom: 0.5vh;
    line-height: 1.4;
}

.contact-section .reg-number {
    font-size: clamp(0.75rem, 1.4vw, 0.9rem);
    color: #777;
    font-style: italic;
}

.contact-section i {
    color: #4CAF50;
    margin-right: 8px;
    width: 20px;
    text-align: center;
}
/* ============================================
   NEW 25-SLIDE LAYOUT STYLES
   ============================================ */

/* Package Simple Layout (Slides 4-5) */
.package-simple {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
    gap: 2vh 2vw;
    max-width: 1200px;
    margin: 0 auto;
    padding: 1vh 2vw;
}

.component-large {
    padding: 2vh 2vw;
    background: linear-gradient(135deg, rgba(76, 175, 80, 0.08), rgba(76, 175, 80, 0.02));
    border: 2px solid rgba(76, 175, 80, 0.3);
    border-radius: 15px;
    text-align: center;
    transition: all 0.4s ease;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
}

.component-large:hover {
    transform: translateY(-5px);
    border-color: #4CAF50;
    box-shadow: 0 10px 30px rgba(76, 175, 80, 0.3);
    background: linear-gradient(135deg, rgba(76, 175, 80, 0.12), rgba(76, 175, 80, 0.04));
}

.component-icon {
    font-size: clamp(2rem, 4vw, 2.8rem);
    color: #4CAF50;
    margin-bottom: 1.5vh;
}

.component-amount {
    font-size: clamp(1.6rem, 3.2vw, 2.2rem);
    font-weight: 800;
    color: #fff;
    margin-bottom: 1vh;
}

.component-label {
    font-size: clamp(1.1rem, 2vw, 1.4rem);
    font-weight: 600;
    color: #4CAF50;
    margin-bottom: 1vh;
}

.component-desc {
    font-size: clamp(0.85rem, 1.6vw, 1rem);
    color: #aaa;
    line-height: 1.5;
}

/* Opportunity Single Layout (Slides 6-7) */
.opportunity-single {
    max-width: 1000px;
    margin: 0 auto;
    padding: 1vh 2vw;
}

.opportunity-header {
    text-align: center;
    margin-bottom: 2vh;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 1.5vw;
}

.opportunity-header i {
    font-size: clamp(1.5rem, 3vw, 2rem);
    color: #4CAF50;
}

.opportunity-header h3 {
    font-size: clamp(1.2rem, 2.4vw, 1.6rem);
    color: #fff;
    font-weight: 700;
}

.stats-large {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: clamp(1.5vh, 2vh, 2.5vh) clamp(1.5vw, 2vw, 2.5vw);
    max-width: 100%;
}

.stat-large {
    padding: clamp(2vh, 2.5vh, 3vh) clamp(1.5vw, 2vw, 2.5vw);
    background: rgba(255, 255, 255, 0.02);
    border: 2px solid rgba(76, 175, 80, 0.25);
    border-radius: 15px;
    text-align: center;
    transition: all 0.3s ease;
    min-height: clamp(100px, 12vh, 130px);
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
}

.stat-large:hover {
    transform: translateY(-5px);
    border-color: #4CAF50;
    box-shadow: 0 12px 35px rgba(76, 175, 80, 0.25);
}

.stat-large-label {
    font-size: clamp(0.75rem, 1.4vw, 0.9rem);
    color: #aaa;
    margin-bottom: clamp(0.5vh, 1vh, 1.5vh);
    font-weight: 500;
    line-height: 1.3;
    word-wrap: break-word;
    max-width: 100%;
}

.stat-large-value {
    font-size: clamp(1.4rem, 2.8vw, 2rem);
    font-weight: 900;
    color: #fff;
    margin-bottom: 0;
    white-space: nowrap;
    overflow: visible;
}

.stat-large-value.highlight {
    color: #4CAF50;
    text-shadow: 0 0 20px rgba(76, 175, 80, 0.5);
}

/* Streams Grid Large (Slides 9-10) */
.streams-grid-large {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
    gap: 2vh 2vw;
    max-width: 1200px;
    margin: 0 auto;
    padding: 1vh 2vw;
}

.stream-card-large {
    padding: 2vh 2vw;
    background: linear-gradient(135deg, rgba(76, 175, 80, 0.08), rgba(76, 175, 80, 0.02));
    border: 2px solid rgba(76, 175, 80, 0.3);
    border-radius: 15px;
    text-align: center;
    cursor: pointer;
    transition: all 0.4s ease;
    display: flex;
    flex-direction: column;
    align-items: center;
}

.stream-card-large:hover {
    transform: translateY(-5px) scale(1.02);
    border-color: #4CAF50;
    box-shadow: 0 15px 40px rgba(76, 175, 80, 0.4);
}

.stream-icon {
    font-size: clamp(2rem, 4vw, 2.8rem);
    color: #4CAF50;
    margin-bottom: 1.5vh;
}

.stream-card-large h3 {
    font-size: clamp(1.2rem, 2.2vw, 1.5rem);
    color: #fff;
    margin-bottom: 1.5vh;
    font-weight: 700;
}

.stream-percentage-large {
    font-size: clamp(1.6rem, 3.2vw, 2.2rem);
    font-weight: 900;
    color: #4CAF50;
    margin-bottom: 0.75vh;
}

.stream-amount-large {
    font-size: clamp(1rem, 1.9vw, 1.2rem);
    color: #aaa;
    margin-bottom: 1.5vh;
    font-weight: 600;
}

.stream-description {
    font-size: clamp(0.85rem, 1.6vw, 1rem);
    color: #bbb;
    line-height: 1.5;
    margin-top: auto;
}

/* Budget Single Layout (Slide 12) */
.budget-single {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0.5vh 2vw;
}

.budget-single h3 {
    font-size: clamp(1rem, 2vw, 1.3rem);
    color: #4CAF50;
    text-align: center;
    margin-bottom: 1vh;
    font-weight: 700;
}

.budget-items-large {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 1.2vh 2vw;
}

.budget-item-large {
    padding: 1.2vh 2vw;
    background: rgba(255, 255, 255, 0.02);
    border: 2px solid rgba(76, 175, 80, 0.25);
    border-radius: 12px;
    transition: all 0.3s ease;
}

.budget-item-large:hover {
    border-color: #4CAF50;
    transform: translateX(5px);
    box-shadow: 0 8px 25px rgba(76, 175, 80, 0.2);
}

.budget-category {
    font-size: clamp(0.95rem, 1.8vw, 1.2rem);
    color: #4CAF50;
    font-weight: 700;
    margin-bottom: 0.5vh;
}

.budget-percentage {
    font-size: clamp(0.75rem, 1.4vw, 0.9rem);
    color: #888;
    margin-bottom: 0.3vh;
}

.budget-amount {
    font-size: clamp(1.2rem, 2.4vw, 1.6rem);
    font-weight: 900;
    color: #fff;
    margin-bottom: 0.5vh;
}

.budget-breakdown {
    font-size: clamp(0.7rem, 1.3vw, 0.85rem);
    color: #aaa;
    line-height: 1.3;
}

/* Funding Structure (Slide 13) */
.funding-structure {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
    gap: 4vh 4vw;
    max-width: 1100px;
    margin: 0 auto;
    padding: 2vh 2vw;
}

.funding-box-large {
    padding: 4vh 3vw;
    border-radius: 20px;
    text-align: center;
    transition: all 0.4s ease;
    position: relative;
    overflow: hidden;
}

.funding-box-large.idc {
    background: rgba(33, 150, 243, 0.08);
    border: 3px solid rgba(33, 150, 243, 0.4);
}

.funding-box-large.grants {
    background: rgba(76, 175, 80, 0.08);
    border: 3px solid rgba(76, 175, 80, 0.4);
}

.funding-box-large:hover {
    transform: translateY(-10px);
    box-shadow: 0 20px 50px rgba(76, 175, 80, 0.3);
}

.funding-icon {
    font-size: clamp(2.5rem, 5vw, 3.5rem);
    margin-bottom: 2vh;
}

.funding-box-large.idc .funding-icon {
    color: #2196F3;
}

.funding-box-large.grants .funding-icon {
    color: #4CAF50;
}

.funding-box-large .funding-header {
    font-size: clamp(1.3rem, 2.4vw, 1.7rem);
    font-weight: 700;
    margin-bottom: 2vh;
}

.funding-box-large.idc .funding-header {
    color: #2196F3;
}

.funding-box-large.grants .funding-header {
    color: #4CAF50;
}

.funding-amount-large {
    font-size: clamp(2.5rem, 5vw, 3.5rem);
    font-weight: 900;
    color: #fff;
    margin-bottom: 3vh;
}

.funding-details-large {
    display: grid;
    gap: 2vh;
    text-align: left;
}

.funding-detail-item {
    display: flex;
    align-items: center;
    gap: 1.5vw;
    padding: 1.5vh 2vw;
    background: rgba(255, 255, 255, 0.03);
    border-radius: 12px;
    font-size: clamp(1rem, 1.9vw, 1.2rem);
    color: #ccc;
}

.funding-detail-item i {
    font-size: clamp(1.2rem, 2.2vw, 1.5rem);
    min-width: 30px;
}

.funding-box-large.idc .funding-detail-item i {
    color: #2196F3;
}

.funding-box-large.grants .funding-detail-item i {
    color: #4CAF50;
}

/* Timeline Table Large (Slide 14) */
.timeline-table-large {
    max-width: 1200px;
    margin: 0 auto;
    padding: 2vh 2vw;
}

.timeline-row-large {
    display: grid;
    grid-template-columns: 100px 1fr 1fr 1fr 1.5fr;
    gap: 2vw;
    padding: 2.5vh 2vw;
    border-bottom: 2px solid rgba(76, 175, 80, 0.2);
    align-items: center;
    transition: all 0.3s ease;
    cursor: pointer;
    font-size: clamp(1rem, 1.9vw, 1.25rem);
}

.timeline-row-large.header {
    background: rgba(76, 175, 80, 0.1);
    border-radius: 12px 12px 0 0;
    font-weight: 700;
    color: #4CAF50;
    cursor: default;
    border-bottom: 3px solid #4CAF50;
}

.timeline-row-large:not(.header):hover {
    background: rgba(76, 175, 80, 0.08);
    transform: translateX(10px);
    border-left: 4px solid #4CAF50;
    padding-left: calc(2vw - 4px);
}

.year-badge {
    width: 60px;
    height: 60px;
    background: linear-gradient(135deg, #4CAF50, #2E7D32);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: clamp(1.5rem, 3vw, 2rem);
    font-weight: 900;
    color: #fff;
    box-shadow: 0 8px 20px rgba(76, 175, 80, 0.4);
}

.timeline-data {
    color: #ddd;
}

.timeline-data.positive {
    color: #4CAF50;
    font-weight: 700;
}

.timeline-data.negative {
    color: #ff5252;
    font-weight: 700;
}

/* Quarters Container (Slide 15) */
.quarters-container {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 2vh 2vw;
    max-width: 1300px;
    margin: 0 auto;
    padding: 1vh 2vw;
}

.quarter-card {
    background: linear-gradient(135deg, rgba(76, 175, 80, 0.08), rgba(76, 175, 80, 0.02));
    border: 2px solid rgba(76, 175, 80, 0.3);
    border-radius: 15px;
    padding: 1.5vh 2vw;
    transition: all 0.4s ease;
}

.quarter-card:hover {
    transform: translateY(-5px);
    border-color: #4CAF50;
    box-shadow: 0 10px 25px rgba(76, 175, 80, 0.3);
}

.quarter-header {
    display: flex;
    align-items: center;
    gap: 1.5vw;
    margin-bottom: 1.5vh;
    padding-bottom: 1vh;
    border-bottom: 2px solid rgba(76, 175, 80, 0.3);
}

.quarter-badge {
    width: 40px;
    height: 40px;
    background: linear-gradient(135deg, #4CAF50, #2E7D32);
    border-radius: 10px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: clamp(1rem, 1.9vw, 1.2rem);
    font-weight: 900;
    color: #fff;
    flex-shrink: 0;
}

.quarter-name {
    font-size: clamp(1.1rem, 2vw, 1.4rem);
    font-weight: 700;
    color: #4CAF50;
}

.quarter-content {
    display: grid;
    gap: 1vh;
}

.quarter-item {
    display: flex;
    align-items: center;
    gap: 1vw;
    padding: 1vh 1.5vw;
    background: rgba(255, 255, 255, 0.02);
    border-radius: 10px;
    font-size: clamp(0.8rem, 1.5vw, 0.95rem);
    color: #ddd;
    transition: all 0.3s ease;
}

.quarter-item:hover {
    background: rgba(76, 175, 80, 0.1);
    transform: translateX(5px);
}

.quarter-item i {
    color: #4CAF50;
    font-size: clamp(0.9rem, 1.7vw, 1.1rem);
    min-width: 20px;
    flex-shrink: 0;
}

/* Impact Single & Grid Large (Slides 16-17) */
.impact-single {
    max-width: 1300px;
    margin: 0 auto;
    padding: 0.5vh 2vw;
}

.impact-header {
    text-align: center;
    margin-bottom: 1.5vh;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 1vw;
}

.impact-header i {
    font-size: clamp(1.3rem, 2.5vw, 1.7rem);
    color: #4CAF50;
}

.impact-header h3 {
    font-size: clamp(1rem, 2vw, 1.3rem);
    color: #fff;
    font-weight: 700;
}

.impact-grid-large {
    display: grid;
    grid-template-columns: repeat(5, 1fr);
    gap: 1.5vh 1.5vw;
}

.impact-card {
    padding: 1.5vh 1.5vw;
    background: rgba(255, 255, 255, 0.02);
    border: 2px solid rgba(76, 175, 80, 0.25);
    border-radius: 12px;
    text-align: center;
    transition: all 0.4s ease;
}

.impact-card:hover {
    transform: translateY(-3px);
    border-color: #4CAF50;
    box-shadow: 0 10px 25px rgba(76, 175, 80, 0.3);
}

.impact-icon {
    width: 45px;
    height: 45px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: clamp(1.3rem, 2.5vw, 1.7rem);
    margin: 0 auto 1vh;
}

.impact-icon.economic {
    background: rgba(33, 150, 243, 0.15);
    color: #2196F3;
}

.impact-icon.environmental {
    background: rgba(76, 175, 80, 0.15);
    color: #4CAF50;
}

.impact-title {
    font-size: clamp(0.75rem, 1.4vw, 0.9rem);
    color: #aaa;
    margin-bottom: 0.75vh;
    font-weight: 500;
}

.impact-value {
    font-size: clamp(1.2rem, 2.3vw, 1.6rem);
    font-weight: 900;
    color: #fff;
    margin-bottom: 0.5vh;
}

.impact-change {
    font-size: clamp(0.7rem, 1.3vw, 0.85rem);
    color: #4CAF50;
    font-weight: 600;
}

.carbon-box {
    margin-top: 4vh;
    padding: 2.5vh 3vw;
    background: linear-gradient(135deg, rgba(76, 175, 80, 0.15), rgba(76, 175, 80, 0.05));
    border: 2px solid rgba(76, 175, 80, 0.4);
    border-radius: 15px;
    text-align: center;
    font-size: clamp(1.1rem, 2.1vw, 1.4rem);
    color: #ddd;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 2vw;
}

.carbon-box i {
    font-size: clamp(1.5rem, 3vw, 2rem);
    color: #4CAF50;
}

/* Moats Large (Slides 18-19) */
.moats-large {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 1.5vh 2vw;
    max-width: 1300px;
    margin: 0 auto;
    padding: 0.5vh 2vw;
}

.moat-card-large {
    padding: 1.5vh 2vw;
    background: linear-gradient(135deg, rgba(76, 175, 80, 0.08), rgba(76, 175, 80, 0.02));
    border: 2px solid rgba(76, 175, 80, 0.3);
    border-radius: 12px;
    transition: all 0.4s ease;
    position: relative;
}

.moat-card-large:hover {
    transform: translateY(-3px);
    border-color: #4CAF50;
    box-shadow: 0 10px 30px rgba(76, 175, 80, 0.3);
}

.moat-badge {
    position: absolute;
    top: 1.5vh;
    right: 1.5vw;
    width: 35px;
    height: 35px;
    background: linear-gradient(135deg, #4CAF50, #2E7D32);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: clamp(1rem, 2vw, 1.3rem);
    font-weight: 900;
    color: #fff;
    box-shadow: 0 6px 15px rgba(76, 175, 80, 0.4);
}

.moat-icon {
    font-size: clamp(1.6rem, 3.2vw, 2.2rem);
    color: #4CAF50;
    margin-bottom: 1vh;
}

.moat-card-large h3 {
    font-size: clamp(1rem, 1.9vw, 1.3rem);
    color: #fff;
    margin-bottom: 1vh;
    font-weight: 700;
}

.moat-stat-large {
    font-size: clamp(1.3rem, 2.6vw, 1.8rem);
    font-weight: 900;
    color: #4CAF50;
    margin-bottom: 0.5vh;
}

.moat-vs {
    font-size: clamp(0.75rem, 1.4vw, 0.9rem);
    color: #888;
    margin-bottom: 1vh;
    font-style: italic;
}

.moat-explanation {
    font-size: clamp(0.75rem, 1.4vw, 0.9rem);
    color: #bbb;
    line-height: 1.4;
}

/* Market Single (Slides 20-21) */
.market-single {
    max-width: 1100px;
    margin: 0 auto;
    padding: 1vh 2vw;
}

.market-header {
    text-align: center;
    margin-bottom: 2vh;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 1.5vw;
}

.market-header i {
    font-size: clamp(1.5rem, 3vw, 2rem);
    color: #4CAF50;
}

.market-header h3 {
    font-size: clamp(1.2rem, 2.4vw, 1.6rem);
    color: #fff;
    font-weight: 700;
}

.market-stats-large {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 2vh 2vw;
    margin-bottom: 2vh;
}

.market-card {
    padding: 2vh 2vw;
    background: rgba(255, 255, 255, 0.02);
    border: 2px solid rgba(76, 175, 80, 0.25);
    border-radius: 15px;
    text-align: center;
    transition: all 0.4s ease;
}

.market-card:hover {
    transform: translateY(-5px) scale(1.02);
    border-color: #4CAF50;
    box-shadow: 0 15px 40px rgba(76, 175, 80, 0.35);
}

.market-number-large {
    font-size: clamp(2rem, 4vw, 2.8rem);
    font-weight: 900;
    color: #4CAF50;
    margin-bottom: 1vh;
}

.market-label-large {
    font-size: clamp(1rem, 1.9vw, 1.25rem);
    color: #fff;
    font-weight: 700;
    margin-bottom: 0.75vh;
}

.market-sublabel {
    font-size: clamp(0.8rem, 1.5vw, 0.95rem);
    color: #888;
    font-style: italic;
}

.market-highlight-large {
    padding: 1.5vh 2vw;
    background: linear-gradient(135deg, rgba(76, 175, 80, 0.15), rgba(76, 175, 80, 0.05));
    border: 2px solid rgba(76, 175, 80, 0.4);
    border-radius: 12px;
    text-align: center;
    font-size: clamp(0.9rem, 1.7vw, 1.1rem);
    color: #ddd;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 1.5vw;
    margin-top: 2vh;
}

.market-highlight-large i {
    font-size: clamp(1.2rem, 2.2vw, 1.5rem);
    color: #4CAF50;
}

/* Responsive adjustments for new layouts */
@media (max-width: 768px) {
    .slide {
        padding: 2vh 3vw;
    }

    h2 {
        font-size: clamp(1.3rem, 2.5vw, 1.8rem);
        margin-bottom: 1.5vh;
    }

    .package-simple,
    .streams-grid-large,
    .funding-structure,
    .quarters-container,
    .impact-grid-large,
    .market-stats-large {
        grid-template-columns: 1fr;
    }

    .timeline-row-large {
        grid-template-columns: 60px repeat(4, 1fr);
        font-size: 0.85rem;
    }

    .year-badge {
        width: 45px;
        height: 45px;
        font-size: 1.2rem;
    }

    .component-large,
    .stream-card-large,
    .quarter-card,
    .moat-card-large {
        min-height: auto;
        padding: 1.5vh 2vw;
    }

    .opportunity-header,
    .impact-header,
    .market-header {
        flex-direction: column;
        gap: 1vh;
    }
}

/* ============================================
   LANGUAGE & ACCESSIBILITY SLIDE COMPONENTS
   ============================================ */
.accessibility-hero {
    background: linear-gradient(135deg, rgba(45, 80, 22, 0.4), rgba(90, 140, 47, 0.2));
    border: 2px solid rgba(76, 175, 80, 0.4);
    border-radius: 15px;
    padding: 2vh 2vw;
    margin-bottom: 2vh;
    text-align: center;
}

.accessibility-hero h3 {
    font-size: clamp(1.3rem, 2.5vw, 1.8rem);
    color: #4CAF50;
    margin-bottom: 1vh;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 10px;
}

.accessibility-hero p {
    font-size: clamp(0.95rem, 1.8vw, 1.2rem);
    color: #bbb;
}

.accessibility-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 2vw;
    margin-bottom: 2vh;
}

.language-box, .features-box {
    background: rgba(255, 255, 255, 0.03);
    border: 2px solid rgba(76, 175, 80, 0.25);
    border-left: 5px solid #4CAF50;
    border-radius: 12px;
    padding: 1.5vh 2vw;
}

.language-box h4, .features-box h4 {
    font-size: clamp(1rem, 1.9vw, 1.2rem);
    color: #4CAF50;
    margin-bottom: 1vh;
    display: flex;
    align-items: center;
    gap: 8px;
}

.language-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 0.5vh 1vw;
    font-size: clamp(0.85rem, 1.6vw, 0.95rem);
    color: #ccc;
}


.features-box ul {
    margin: 0;
    padding-left: 1.2rem;
    font-size: clamp(0.85rem, 1.6vw, 0.95rem);
    color: #ccc;
    line-height: 1.7;
}

.features-box li strong {
    color: #4CAF50;
}

.moat-callout {
    background: linear-gradient(135deg, rgba(255, 193, 7, 0.1), rgba(255, 193, 7, 0.05));
    border: 2px solid rgba(255, 193, 7, 0.4);
    border-radius: 12px;
    padding: 1.5vh 2vw;
    text-align: center;
}

.moat-callout strong {
    font-size: clamp(1rem, 1.9vw, 1.2rem);
    color: #FFC107;
}

.moat-callout span {
    font-size: clamp(0.9rem, 1.7vw, 1.05rem);
    color: #ddd;
}

/* ============================================
   REVENUE STREAM INTEGRATION SLIDE
   ============================================ */
.revenue-hero {
    background: linear-gradient(135deg, rgba(45, 80, 22, 0.4), rgba(90, 140, 47, 0.2));
    border: 2px solid rgba(76, 175, 80, 0.4);
    border-radius: 15px;
    padding: 2vh 2vw;
    margin-bottom: 2vh;
    text-align: center;
}

.revenue-hero h3 {
    font-size: clamp(1.5rem, 2.8vw, 2rem);
    color: #4CAF50;
    margin-bottom: 0.5vh;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 10px;
}

.revenue-hero p {
    font-size: clamp(1rem, 1.9vw, 1.3rem);
    color: #bbb;
}

.revenue-streams-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1.5vh 1.5vw;
    margin-bottom: 2vh;
}

.revenue-stream-button {
    background: rgba(255, 255, 255, 0.03);
    border: 2px solid rgba(76, 175, 80, 0.3);
    border-radius: 12px;
    padding: 1.5vh 1.5vw;
    text-align: center;
    cursor: pointer;
    transition: all 0.3s ease;
    position: relative;
    overflow: hidden;
}

.revenue-stream-button::before {
    content: '';
    position: absolute;
    top: 50%;
    left: 50%;
    width: 0;
    height: 0;
    border-radius: 50%;
    background: rgba(76, 175, 80, 0.3);
    transform: translate(-50%, -50%);
    transition: width 0.6s, height 0.6s;
}

.revenue-stream-button:hover::before {
    width: 300px;
    height: 300px;
}

.revenue-stream-button:hover {
    transform: translateY(-5px);
    border-color: #4CAF50;
    box-shadow: 0 0 25px rgba(76, 175, 80, 0.6),
                0 0 50px rgba(76, 175, 80, 0.4),
                0 0 75px rgba(76, 175, 80, 0.2);
}

.revenue-stream-button .emoji {
    font-size: clamp(1.8rem, 3.5vw, 2.5rem);
    margin-bottom: 0.5vh;
    display: block;
}

.revenue-stream-button h4 {
    font-size: clamp(0.95rem, 1.8vw, 1.2rem);
    color: #fff;
    margin: 0.5vh 0;
    font-weight: 700;
}

.revenue-stream-button p {
    font-size: clamp(0.75rem, 1.4vw, 0.85rem);
    color: #aaa;
    margin: 0;
    line-height: 1.4;
}

.revenue-insight {
    background: linear-gradient(135deg, rgba(255, 193, 7, 0.1), rgba(255, 193, 7, 0.05));
    border: 2px solid rgba(255, 193, 7, 0.4);
    border-radius: 12px;
    padding: 1.5vh 2vw;
    text-align: center;
}

.revenue-insight strong {
    font-size: clamp(1rem, 1.9vw, 1.25rem);
    color: #FFC107;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 8px;
    margin-bottom: 0.5vh;
}

.revenue-insight p {
    font-size: clamp(0.85rem, 1.6vw, 1.05rem);
    color: #ddd;
    margin: 0.5vh 0 0 0;
    line-height: 1.5;
}

.revenue-insight .highlight {
    color: #4CAF50;
    font-weight: 700;
}

/* ============================================
   ENHANCED MODAL ANIMATIONS & ELEMENTS
   ============================================ */
@keyframes fadeIn {
    from {
        opacity: 0;
        transform: translateY(20px);
    }
    to {
        opacity: 1;
        transform: translateY(0);
    }
}

@keyframes slideInUp {
    from {
        opacity: 0;
        transform: translateY(clamp(20px, 3vh, 40px)) scale(0.95);
    }
    to {
        opacity: 1;
        transform: translateY(0) scale(1);
    }
}

.fade-in {
    animation: fadeIn 0.7s cubic-bezier(0.4, 0, 0.2, 1) forwards;
    opacity: 0;
    animation-fill-mode: forwards !important;
}

.modal-stat.enhanced {
    animation: slideInUp 0.6s cubic-bezier(0.4, 0, 0.2, 1) forwards;
    opacity: 0;
    animation-fill-mode: forwards !important;
}

.modal-stat.enhanced.animated {
    opacity: 1;
}

/* Progress Bar Styles */
.progress-bar {
    width: 100%;
    height: clamp(4px, 0.6vh, 8px);
    background: rgba(255, 255, 255, 0.1);
    border-radius: clamp(5px, 1vw, 10px);
    overflow: hidden;
    margin-top: clamp(0.8vh, 1.2vh, 1.5vh);
}

.progress-fill {
    height: 100%;
    width: 0;
    background: linear-gradient(90deg, #4CAF50, #8BC34A);
    border-radius: clamp(5px, 1vw, 10px);
    transition: width 1.8s cubic-bezier(0.4, 0, 0.2, 1);
    box-shadow: 0 0 clamp(8px, 1.5vw, 15px) rgba(76, 175, 80, 0.6);
}

/* Modal Chart Container */
.modal-chart-container {
    max-width: min(50vw, 500px);
    width: 100%;
    margin: clamp(2vh, 3vh, 4vh) auto;
    padding: clamp(1.5vh, 2vh, 2.5vh) clamp(1.5vw, 2vw, 2.5vw);
    background: rgba(255, 255, 255, 0.03);
    border-radius: clamp(12px, 1.5vw, 18px);
    border: 1px solid rgba(76, 175, 80, 0.2);
    display: flex;
    align-items: center;
    justify-content: center;
}

.modal-chart-container canvas {
    max-width: 100%;
    height: auto !important;
}

.modal-stat-value {
    transition: all 0.3s ease;
}

.modal-stat-value:hover {
    transform: scale(1.05);
    color: #8BC34A;
}

/* Modal Stats Grid Enhanced */
.modal-stat::before {
    content: '';
    position: absolute;
    top: -5px;
    left: -5px;
    right: -5px;
    bottom: -5px;
    background: linear-gradient(135deg, rgba(76, 175, 80, 0.2), transparent);
    border-radius: clamp(12px, 1.5vw, 18px);
    opacity: 0;
    transition: opacity 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    z-index: -1;
}

.modal-stat:hover::before {
    opacity: 1;
}

/* Pulse animation for interactive elements */
@keyframes pulse {
    0%, 100% {
        box-shadow: 0 0 0 0 rgba(76, 175, 80, 0.7);
    }
    50% {
        box-shadow: 0 0 0 15px rgba(76, 175, 80, 0);
    }
}

.modal-stat.enhanced:hover {
    animation: pulse 1.5s infinite;
    opacity: 1 !important;
}

/* ============================================
   COMPREHENSIVE RESPONSIVE CARD FIXES
   ============================================ */
@media (max-width: 1024px) {
    .stats-grid {
        grid-template-columns: repeat(2, 1fr);
        gap: clamp(2vh, 2.5vh, 3vh) clamp(1.5vw, 2vw, 2.5vw);
    }

    .stats-large {
        grid-template-columns: repeat(2, 1fr);
    }

    .platform-grid {
        grid-template-columns: repeat(2, 1fr);
    }
}

@media (max-width: 768px) {
    .stats-grid {
        grid-template-columns: repeat(2, 1fr);
        gap: 2vh 2vw;
    }

    .stat-card {
        min-height: clamp(140px, 18vh, 180px);
        padding: 2.5vh 2vw;
    }

    .stat-card .number {
        font-size: clamp(1.8rem, 5vw, 3.5rem);
    }

    .stat-card .label {
        font-size: clamp(0.75rem, 1.8vw, 1rem);
    }

    .platform-grid {
        grid-template-columns: 1fr;
    }

    .platform-card {
        min-height: clamp(120px, 16vh, 160px);
    }

    .modal-chart-container {
        max-width: 85vw;
        padding: 2vh 3vw;
    }

    .accessibility-grid, .revenue-streams-grid {
        grid-template-columns: 1fr;
    }

    .revenue-streams-grid {
        grid-template-columns: repeat(2, 1fr);
    }
}

@media (max-width: 480px) {
    .stats-grid {
        grid-template-columns: 1fr;
    }

    .stat-card {
        min-height: auto;
        padding: 3vh 4vw;
    }
}
/* ============================================
   LANDSCAPE OPTIMIZATION - SLIDE 2 (CRISIS)
   ============================================ */
.crisis-stat-large {
    text-align: center;
    margin-bottom: 2vh !important;
    padding: 1vh 0 !important;
}

.crisis-number-huge {
    font-size: clamp(3.5rem, 7vw, 6rem) !important;
    font-weight: 900;
    color: #ff5252;
    line-height: 1;
    margin-bottom: 1vh;
}

.crisis-label-large {
    font-size: clamp(0.95rem, 1.8vw, 1.2rem) !important;
    color: #999;
}

.failure-flow {
    display: flex !important;
    flex-direction: row !important;
    align-items: center !important;
    justify-content: center !important;
    gap: clamp(2vw, 3vw, 4vw) !important;
    margin: 2vh auto !important;
    max-width: 95% !important;
}

.failure-step {
    flex: 1 !important;
    max-width: 280px !important;
    padding: 2vh 1.5vw !important;
    background: rgba(255, 255, 255, 0.02) !important;
    border: 2px solid rgba(76, 175, 80, 0.2) !important;
    border-radius: 12px !important;
    text-align: center !important;
}

.failure-step.farmer {
    border-color: rgba(255, 193, 7, 0.3) !important;
    background: rgba(255, 193, 7, 0.03) !important;
}

.failure-step.fail {
    border-color: rgba(255, 82, 82, 0.3) !important;
    background: rgba(255, 82, 82, 0.03) !important;
}

.failure-icon {
    font-size: clamp(2rem, 3.5vw, 2.8rem) !important;
    margin-bottom: 1vh !important;
    color: #4CAF50;
}

.failure-step.farmer .failure-icon {
    color: #FFC107;
}

.failure-step.fail .failure-icon {
    color: #ff5252;
}

.failure-step h3 {
    font-size: clamp(1.1rem, 2vw, 1.4rem) !important;
    margin-bottom: 1vh !important;
    color: #fff !important;
}

.failure-step p {
    font-size: clamp(0.85rem, 1.5vw, 1rem) !important;
    color: #aaa;
    line-height: 1.4;
}

.failure-arrow {
    font-size: clamp(1.8rem, 3vw, 2.5rem) !important;
    color: #4CAF50;
    flex-shrink: 0;
}

.insight-box {
    display: flex;
    align-items: center;
    gap: 1.5vw;
    padding: 2vh 2.5vw !important;
    background: rgba(76, 175, 80, 0.08);
    border: 2px solid #4CAF50;
    border-radius: 12px;
    margin-top: 2vh !important;
}

.insight-box i {
    font-size: clamp(1.8rem, 3vw, 2.5rem);
    color: #4CAF50;
    flex-shrink: 0;
}

.insight-text {
    font-size: clamp(1rem, 1.8vw, 1.2rem) !important;
    color: #ccc;
    line-height: 1.5;
}

/* ============================================
   LANDSCAPE OPTIMIZATION - SLIDE 3 (INNOVATION)
   ============================================ */
.innovation-section {
    text-align: center;
    margin-bottom: 2vh !important;
    padding: 1.5vh 2vw;
    background: rgba(76, 175, 80, 0.05);
    border-radius: 12px;
}

.innovation-section h3 {
    font-size: clamp(1.3rem, 2.5vw, 1.8rem) !important;
    color: #4CAF50;
    margin-bottom: 0.5vh !important;
}

.innovation-desc {
    font-size: clamp(0.9rem, 1.6vw, 1.1rem) !important;
    color: #aaa;
    margin: 0 !important;
}

.platform-grid {
    display: grid !important;
    grid-template-columns: repeat(3, 1fr) !important;
    gap: 1.5vh 2vw !important;
    margin-top: 2vh !important;
    max-height: 50vh !important;
}

.platform-item {
    padding: 2vh 1.5vw !important;
    background: rgba(255, 255, 255, 0.02) !important;
    border: 2px solid rgba(76, 175, 80, 0.2) !important;
    border-radius: 12px !important;
    text-align: center !important;
    transition: all 0.3s ease;
}

.platform-item:hover {
    background: rgba(76, 175, 80, 0.05) !important;
    border-color: #4CAF50 !important;
    transform: translateY(-3px);
}

.platform-item .platform-icon {
    font-size: clamp(1.8rem, 3vw, 2.5rem) !important;
    color: #4CAF50;
    margin-bottom: 1vh !important;
}

.platform-item h4 {
    font-size: clamp(1rem, 1.8vw, 1.3rem) !important;
    color: #fff;
    margin-bottom: 0.5vh !important;
}

.platform-item p {
    font-size: clamp(0.8rem, 1.4vw, 0.95rem) !important;
    color: #999;
    margin: 0;
    line-height: 1.4;
}

.genius-callout {
    text-align: center;
    padding: 1.5vh 2.5vw !important;
    background: rgba(76, 175, 80, 0.1);
    border: 2px solid #4CAF50;
    border-radius: 12px;
    margin-top: 2vh !important;
    font-size: clamp(1rem, 1.9vw, 1.3rem) !important;
    color: #4CAF50;
}

/* ============================================
   LANDSCAPE OPTIMIZATION - SLIDE 4 (COMPONENT)
   ============================================ */
.component-explanation {
    display: grid !important;
    grid-template-columns: 1fr 1.5fr !important;
    gap: 3vw !important;
    margin: 2vh 0 !important;
    align-items: start !important;
}

.component-left {
    text-align: center;
    padding: 2vh 1.5vw;
    background: rgba(76, 175, 80, 0.05);
    border-radius: 12px;
    display: flex;
    flex-direction: column;
    justify-content: center;
}

.component-stat .stat-value {
    font-size: clamp(3rem, 6vw, 5rem) !important;
    font-weight: 900;
    color: #4CAF50;
    line-height: 1;
    margin-bottom: 1vh;
}

.component-stat .stat-label {
    font-size: clamp(1rem, 1.8vw, 1.3rem) !important;
    color: #aaa;
    margin-bottom: 2vh;
}

.component-desc {
    font-size: clamp(0.95rem, 1.7vw, 1.15rem) !important;
    color: #ccc;
    line-height: 1.5;
}

.component-right h3 {
    font-size: clamp(1.1rem, 2vw, 1.4rem) !important;
    color: #fff;
    margin-bottom: 1vh !important;
}

.not-list, .is-list {
    list-style: none;
    margin-bottom: 2vh !important;
}

.not-list li, .is-list li {
    display: flex;
    align-items: flex-start;
    gap: 1vw;
    margin-bottom: 0.8vh !important;
    font-size: clamp(0.85rem, 1.5vw, 1rem) !important;
    color: #ccc;
    line-height: 1.4;
}

.not-list i {
    color: #ff5252;
    flex-shrink: 0;
    margin-top: 0.3vh;
}

.is-list i {
    color: #4CAF50;
    flex-shrink: 0;
    margin-top: 0.3vh;
}

.analogy-box {
    text-align: center;
    padding: 1.5vh 2.5vw !important;
    background: rgba(33, 150, 243, 0.08);
    border: 2px solid rgba(33, 150, 243, 0.3);
    border-radius: 12px;
    margin-top: 2vh !important;
    font-size: clamp(0.95rem, 1.7vw, 1.15rem) !important;
    color: #ccc;
    line-height: 1.5;
}

/* ============================================
   LANDSCAPE OPTIMIZATION - SLIDE 5 (BUDGET)
   ============================================ */
.budget-visual {
    display: flex !important;
    flex-direction: column !important;
    gap: 1.2vh !important;
    margin: 2vh auto !important;
    max-width: 100% !important;
}

.budget-bar-item {
    display: flex;
    flex-direction: column;
    gap: 0.5vh;
}

.budget-bar-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 0.3vh;
}

.budget-category {
    font-size: clamp(0.85rem, 1.5vw, 1rem) !important;
    color: #ccc;
    font-weight: 600;
}

.budget-amount {
    font-size: clamp(0.95rem, 1.7vw, 1.2rem) !important;
    color: #4CAF50;
    font-weight: 700;
}

.budget-bar {
    width: 100%;
    height: clamp(20px, 2.5vh, 28px) !important;
    background: rgba(255, 255, 255, 0.05);
    border-radius: 8px;
    overflow: hidden;
    position: relative;
}

.budget-fill {
    height: 100%;
    background: linear-gradient(90deg, #4CAF50, #2E7D32);
    border-radius: 8px;
    transition: width 0.5s ease;
}

.budget-percent {
    font-size: clamp(0.75rem, 1.3vw, 0.9rem) !important;
    color: #888;
    text-align: right;
}

.budget-footer {
    text-align: center;
    padding: 1.5vh 2vw !important;
    background: rgba(76, 175, 80, 0.08);
    border-radius: 12px;
    margin-top: 2vh !important;
    font-size: clamp(0.9rem, 1.6vw, 1.1rem) !important;
    color: #ccc;
}

/* ============================================
   LANDSCAPE OPTIMIZATION - SLIDE 6 (ROADMAP)
   ============================================ */
.timeline-phases {
    display: grid !important;
    grid-template-columns: repeat(3, 1fr) !important;
    gap: 2vh 2.5vw !important;
    margin: 2vh auto !important;
    max-width: 100% !important;
}

.phase-card {
    padding: 2vh 1.5vw !important;
    background: rgba(255, 255, 255, 0.02);
    border: 2px solid rgba(76, 175, 80, 0.25);
    border-radius: 12px;
    transition: all 0.3s ease;
}

.phase-card:hover {
    background: rgba(76, 175, 80, 0.05);
    border-color: #4CAF50;
    transform: translateY(-5px);
}

.phase-header {
    display: flex;
    align-items: center;
    gap: 1vw;
    margin-bottom: 1vh !important;
}

.phase-number {
    width: clamp(35px, 4vw, 45px);
    height: clamp(35px, 4vw, 45px);
    background: linear-gradient(135deg, #4CAF50, #2E7D32);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: clamp(1rem, 1.8vw, 1.3rem) !important;
    font-weight: 900;
    flex-shrink: 0;
}

.phase-title {
    font-size: clamp(1.1rem, 2vw, 1.5rem) !important;
    color: #fff;
    font-weight: 700;
}

.phase-timeline {
    font-size: clamp(0.8rem, 1.4vw, 0.95rem) !important;
    color: #4CAF50;
    margin-bottom: 0.5vh !important;
}

.phase-budget {
    font-size: clamp(1.2rem, 2.2vw, 1.6rem) !important;
    color: #4CAF50;
    font-weight: 800;
    margin-bottom: 1vh !important;
}

.phase-milestones {
    list-style: none;
    margin: 0;
    padding: 0;
}

.phase-milestones li {
    display: flex;
    align-items: flex-start;
    gap: 0.8vw;
    margin-bottom: 0.8vh !important;
    font-size: clamp(0.75rem, 1.3vw, 0.9rem) !important;
    color: #ccc;
    line-height: 1.4;
}

.phase-milestones i {
    color: #4CAF50;
    flex-shrink: 0;
    margin-top: 0.2vh;
    font-size: clamp(0.7rem, 1.2vw, 0.85rem);
}

/* ============================================
   LANDSCAPE OPTIMIZATION - SLIDE 7 (VALIDATION)
   ============================================ */
.validation-grid {
    display: grid !important;
    grid-template-columns: repeat(2, 1fr) !important;
    gap: 2vh 2.5vw !important;
    margin: 2vh auto !important;
    max-width: 100% !important;
}

.validation-card {
    padding: 2vh 2vw !important;
    background: rgba(255, 255, 255, 0.02);
    border: 2px solid rgba(76, 175, 80, 0.25);
    border-radius: 12px;
    transition: all 0.3s ease;
}

.validation-card.can {
    border-color: rgba(76, 175, 80, 0.3);
}

.validation-card:hover {
    background: rgba(76, 175, 80, 0.05);
    border-color: #4CAF50;
    transform: translateY(-3px);
}

.validation-header {
    display: flex;
    align-items: center;
    gap: 1vw;
    margin-bottom: 1.5vh !important;
}

.validation-header i {
    font-size: clamp(1.5rem, 2.5vw, 2rem) !important;
    color: #4CAF50;
    flex-shrink: 0;
}

.validation-header h3 {
    font-size: clamp(1rem, 1.8vw, 1.3rem) !important;
    color: #fff;
    margin: 0 !important;
}

.validation-card ul {
    list-style: none;
    margin: 0;
    padding: 0;
}

.validation-card li {
    margin-bottom: 1vh !important;
    padding-left: 1.2em;
    position: relative;
    font-size: clamp(0.8rem, 1.4vw, 0.95rem) !important;
    color: #ccc;
    line-height: 1.5;
}

.validation-card li::before {
    content: '•';
    position: absolute;
    left: 0;
    color: #4CAF50;
    font-weight: bold;
}

.validation-footer {
    text-align: center;
    padding: 1.5vh 2.5vw !important;
    background: rgba(76, 175, 80, 0.08);
    border-radius: 12px;
    margin-top: 2vh !important;
    font-size: clamp(0.95rem, 1.7vw, 1.15rem) !important;
    color: #ccc;
}

/* ============================================
   LANDSCAPE OPTIMIZATION - SLIDE 8 (IMPACT)
   ============================================ */
.impact-metrics {
    display: flex;
    flex-direction: column;
    gap: 2vh !important;
    margin: 2vh auto !important;
    max-width: 100% !important;
}

.impact-row {
    display: flex !important;
    flex-direction: row !important;
    gap: 2vw !important;
    justify-content: center !important;
    align-items: stretch !important;
}

.impact-metric {
    flex: 1 !important;
    max-width: 250px !important;
    padding: 2vh 1.5vw !important;
    background: rgba(255, 255, 255, 0.02);
    border: 2px solid rgba(76, 175, 80, 0.25);
    border-radius: 12px;
    text-align: center;
    transition: all 0.3s ease;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    gap: 1vh;
}

.impact-metric.financial {
    border-color: rgba(76, 175, 80, 0.3);
}

.impact-metric.social {
    border-color: rgba(33, 150, 243, 0.3);
}

.impact-metric.environmental {
    border-color: rgba(139, 195, 74, 0.3);
}

.impact-metric:hover {
    transform: translateY(-5px);
    border-color: #4CAF50;
    box-shadow: 0 10px 30px rgba(76, 175, 80, 0.2);
}

.impact-icon {
    font-size: clamp(1.8rem, 3vw, 2.5rem) !important;
    color: #4CAF50;
    margin-bottom: 0.5vh !important;
}

.impact-value {
    font-size: clamp(1.5rem, 2.8vw, 2.2rem) !important;
    font-weight: 900;
    color: #fff;
    line-height: 1.1;
}

.impact-label {
    font-size: clamp(0.8rem, 1.4vw, 0.95rem) !important;
    color: #999;
    text-transform: uppercase;
    letter-spacing: 0.5px;
}

.impact-statement {
    text-align: center;
    padding: 2vh 2.5vw !important;
    background: rgba(76, 175, 80, 0.08);
    border-radius: 12px;
    margin-top: 2vh !important;
}

.impact-statement p {
    font-size: clamp(0.95rem, 1.7vw, 1.15rem) !important;
    color: #ccc;
    line-height: 1.6;
    margin: 0;
}

/* ============================================
   LANDSCAPE OPTIMIZATION - SLIDE 9 (WHY GAUTENG)
   ============================================ */
.why-gauteng {
    display: flex;
    flex-direction: column;
    gap: 2vh !important;
    margin: 2vh 0 !important;
}

.why-section {
    padding: 2vh 2vw;
    background: rgba(255, 255, 255, 0.02);
    border-radius: 12px;
}

.why-section h3 {
    font-size: clamp(1.2rem, 2.2vw, 1.6rem) !important;
    color: #4CAF50;
    margin-bottom: 1.5vh !important;
    display: flex;
    align-items: center;
    gap: 1vw;
}

.why-grid {
    display: grid !important;
    grid-template-columns: repeat(2, 1fr) !important;
    gap: 1.5vh 2vw !important;
}

.why-item {
    padding: 1.5vh 1.5vw;
    background: rgba(76, 175, 80, 0.03);
    border: 2px solid rgba(76, 175, 80, 0.2);
    border-radius: 10px;
    transition: all 0.3s ease;
}

.why-item:hover {
    background: rgba(76, 175, 80, 0.08);
    border-color: #4CAF50;
    transform: translateX(5px);
}

.why-item strong {
    display: block;
    font-size: clamp(0.95rem, 1.7vw, 1.15rem) !important;
    color: #4CAF50;
    margin-bottom: 0.5vh !important;
}

.why-item p {
    font-size: clamp(0.8rem, 1.4vw, 0.95rem) !important;
    color: #ccc;
    line-height: 1.5;
    margin: 0;
}

.urgency-list {
    display: flex !important;
    flex-direction: column !important;
    gap: 1.2vh !important;
}

.urgency-item {
    display: flex !important;
    align-items: flex-start !important;
    gap: 1.5vw !important;
    padding: 1.5vh 1.5vw;
    background: rgba(255, 193, 7, 0.03);
    border: 2px solid rgba(255, 193, 7, 0.2);
    border-radius: 10px;
    transition: all 0.3s ease;
}

.urgency-item:hover {
    background: rgba(255, 193, 7, 0.08);
    border-color: #FFC107;
    transform: translateX(5px);
}

.urgency-item i {
    font-size: clamp(1.5rem, 2.5vw, 2rem) !important;
    color: #FFC107;
    flex-shrink: 0;
    margin-top: 0.3vh;
}

.urgency-item strong {
    display: block;
    font-size: clamp(0.95rem, 1.7vw, 1.15rem) !important;
    color: #FFC107;
    margin-bottom: 0.3vh !important;
}

.urgency-item p {
    font-size: clamp(0.8rem, 1.4vw, 0.95rem) !important;
    color: #ccc;
    line-height: 1.5;
    margin: 0;
}

.value-prop {
    text-align: center;
    padding: 2vh 2.5vw !important;
    background: rgba(76, 175, 80, 0.1);
    border: 2px solid #4CAF50;
    border-radius: 12px;
    margin-top: 2vh !important;
    font-size: clamp(1rem, 1.8vw, 1.2rem) !important;
    color: #ccc;
    line-height: 1.6;
}

/* ============================================
   LANDSCAPE OPTIMIZATION - SLIDE 10 (THE ASK)
   ============================================ */
.ask-layout {
    display: grid !important;
    grid-template-columns: 2fr 1fr !important;
    gap: 3vw !important;
    margin: 2vh 0 !important;
    align-items: start !important;
}

.ask-main h1 {
    font-size: clamp(2.5rem, 5vw, 4rem) !important;
    color: #fff;
    margin-bottom: 2vh !important;
}

.ask-amount-box {
    text-align: center;
    padding: 2vh 2vw;
    background: rgba(76, 175, 80, 0.1);
    border: 2px solid #4CAF50;
    border-radius: 12px;
    margin-bottom: 2vh !important;
}

.ask-amount {
    font-size: clamp(2.5rem, 5vw, 4rem) !important;
    font-weight: 900;
    color: #4CAF50;
    line-height: 1;
}

.ask-duration {
    font-size: clamp(1rem, 1.8vw, 1.3rem) !important;
    color: #aaa;
    margin-top: 0.5vh;
}

.ask-outcomes h3 {
    font-size: clamp(1.2rem, 2.2vw, 1.6rem) !important;
    color: #fff;
    margin-bottom: 1.5vh !important;
}

.outcome-grid {
    display: flex;
    flex-direction: column;
    gap: 1vh !important;
    margin-bottom: 2vh !important;
}

.outcome-item {
    display: flex;
    align-items: flex-start;
    gap: 1vw;
    padding: 1vh 1.5vw;
    background: rgba(76, 175, 80, 0.03);
    border-radius: 8px;
    transition: all 0.3s ease;
}

.outcome-item:hover {
    background: rgba(76, 175, 80, 0.08);
    transform: translateX(5px);
}

.outcome-item i {
    color: #4CAF50;
    flex-shrink: 0;
    margin-top: 0.2vh;
    font-size: clamp(0.9rem, 1.5vw, 1.1rem);
}

.outcome-item span {
    font-size: clamp(0.85rem, 1.5vw, 1rem) !important;
    color: #ccc;
    line-height: 1.5;
}

.ask-sequencing {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 1.5vw;
    margin: 2vh 0 !important;
}

.sequence-step {
    text-align: center;
    padding: 1.5vh 1.5vw;
    background: rgba(76, 175, 80, 0.05);
    border: 2px solid rgba(76, 175, 80, 0.25);
    border-radius: 10px;
    flex: 1;
    max-width: 180px;
}

.sequence-label {
    font-size: clamp(0.8rem, 1.4vw, 0.95rem) !important;
    color: #4CAF50;
    font-weight: 700;
    margin-bottom: 0.5vh !important;
}

.sequence-action {
    font-size: clamp(0.85rem, 1.5vw, 1rem) !important;
    color: #ccc;
    line-height: 1.4;
}

.sequence-arrow {
    font-size: clamp(1.5rem, 2.5vw, 2rem) !important;
    color: #4CAF50;
    flex-shrink: 0;
}

.ubuntu-quote-box {
    padding: 2vh 2.5vw !important;
    background: rgba(33, 150, 243, 0.08);
    border: 2px solid rgba(33, 150, 243, 0.3);
    border-radius: 12px;
    text-align: center;
    position: relative;
    margin-top: 2vh !important;
}

.ubuntu-quote-box i {
    font-size: clamp(1.2rem, 2vw, 1.6rem);
    color: rgba(33, 150, 243, 0.4);
}

.ubuntu-quote-box p {
    font-size: clamp(1rem, 1.8vw, 1.3rem) !important;
    color: #ccc;
    font-style: italic;
    line-height: 1.6;
    margin: 1vh 0;
}

.ask-sidebar {
    padding: 2vh 1.5vw;
    background: rgba(255, 255, 255, 0.02);
    border: 2px solid rgba(76, 175, 80, 0.25);
    border-radius: 12px;
}

.contact-info-compact h4 {
    font-size: clamp(1rem, 1.8vw, 1.3rem) !important;
    color: #4CAF50;
    margin-bottom: 1vh !important;
    margin-top: 1.5vh !important;
}

.contact-info-compact h4:first-child {
    margin-top: 0 !important;
}

.contact-info-compact p {
    font-size: clamp(0.8rem, 1.4vw, 0.95rem) !important;
    color: #ccc;
    margin-bottom: 0.5vh !important;
    line-height: 1.5;
}

.contact-info-compact i {
    color: #4CAF50;
    margin-right: 0.5vw;
}

.ask-logo {
    max-width: 120px;
    width: 100%;
    margin-bottom: 2vh;
    filter: drop-shadow(0 5px 15px rgba(76, 175, 80, 0.3));
}

.partners-compact {
    margin-top: 2vh !important;
}

.partner-badge {
    padding: 0.8vh 1vw;
    background: rgba(76, 175, 80, 0.1);
    border: 1px solid rgba(76, 175, 80, 0.3);
    border-radius: 8px;
    font-size: clamp(0.75rem, 1.3vw, 0.85rem) !important;
    color: #4CAF50;
    margin-bottom: 0.5vh;
    text-align: center;
}

.final-cta {
    text-align: center;
    padding: 2vh 2.5vw !important;
    background: rgba(76, 175, 80, 0.1);
    border: 2px solid #4CAF50;
    border-radius: 12px;
    margin-top: 2vh !important;
    font-size: clamp(1.1rem, 2vw, 1.4rem) !important;
    color: #ccc;
}

/* ============================================
   TITLE SLIDE LANDSCAPE OPTIMIZATION
   ============================================ */
.title-grid {
    display: grid !important;
    grid-template-columns: repeat(2, 1fr) !important;
    gap: 2vh 3vw !important;
    max-width: 900px !important;
    margin: 0 auto 3vh auto !important;
}

.title-box {
    padding: 2vh 2vw !important;
    background: rgba(255, 255, 255, 0.02);
    border: 2px solid rgba(76, 175, 80, 0.25);
    border-radius: 12px;
    text-align: center;
    transition: all 0.3s ease;
}

.title-box.primary {
    border-color: #4CAF50;
    background: rgba(76, 175, 80, 0.05);
}

.title-box.secondary {
    border-color: rgba(33, 150, 243, 0.3);
    background: rgba(33, 150, 243, 0.03);
}

.title-box:hover {
    transform: translateY(-5px);
    box-shadow: 0 10px 30px rgba(76, 175, 80, 0.2);
}

.title-label {
    font-size: clamp(0.85rem, 1.5vw, 1rem) !important;
    color: #aaa;
    text-transform: uppercase;
    letter-spacing: 1px;
    margin-bottom: 0.5vh;
}

.title-value {
    font-size: clamp(2.5rem, 5vw, 4rem) !important;
    font-weight: 900;
    color: #4CAF50;
    line-height: 1;
    margin-bottom: 0.5vh;
}

.title-box.secondary .title-value {
    color: #2196F3;
}

.title-desc {
    font-size: clamp(0.8rem, 1.4vw, 0.95rem) !important;
    color: #999;
    line-height: 1.4;
}

.presenter-info {
    font-size: clamp(0.85rem, 1.5vw, 1rem) !important;
    color: #888;
    line-height: 1.6;
}

.presenter-info strong {
    color: #4CAF50;
}

/* ============================================
   GLOBAL LANDSCAPE SPACING REDUCTIONS
   ============================================ */
.slide {
    padding: 2.5vh 3.5vw 10vh 3.5vw !important;
}

h2 {
    margin-bottom: 1.5vh !important;
    padding-bottom: 0.5vh !important;
}
/* ============================================
   SNAKE GAME SCALING - NAVIGATION FIX
   ============================================ */
.navigation {
    position: fixed !important;
    bottom: clamp(15px, 2.5vh, 25px) !important;
    right: clamp(15px, 2.5vw, 25px) !important;
    display: flex !important;
    gap: clamp(8px, 1.5vw, 12px) !important;
    z-index: 99999 !important;
    pointer-events: auto !important;
}

.nav-btn {
    background: rgba(76, 175, 80, 0.2) !important;
    border: 2px solid #4CAF50 !important;
    color: #fff !important;
    padding: clamp(10px, 1.8vh, 14px) clamp(20px, 3.5vw, 28px) !important;
    font-size: clamp(0.85rem, 1.6vw, 1.05rem) !important;
    border-radius: 50px !important;
    cursor: pointer !important;
    transition: all 0.3s ease !important;
    font-weight: 700 !important;
    font-family: 'Poppins', sans-serif !important;
    text-transform: uppercase !important;
    letter-spacing: 1.2px !important;
    pointer-events: auto !important;
    backdrop-filter: blur(10px) !important;
    position: relative !important;
    z-index: 99999 !important;
}

.nav-btn:hover:not(:disabled) {
    background: #4CAF50 !important;
    border-color: #4CAF50 !important;
    transform: translateY(-4px) !important;
    box-shadow: 0 12px 35px rgba(76, 175, 80, 0.45) !important;
}

.slide-counter {
    position: fixed !important;
    bottom: clamp(15px, 2.5vh, 25px) !important;
    left: clamp(15px, 2.5vw, 25px) !important;
    color: #4CAF50 !important;
    font-size: clamp(1.05rem, 2.2vw, 1.4rem) !important;
    font-weight: 700 !important;
    background: rgba(76, 175, 80, 0.2) !important;
    padding: clamp(10px, 1.8vh, 14px) clamp(20px, 3.5vw, 28px) !important;
    border-radius: 50px !important;
    border: 2px solid #4CAF50 !important;
    z-index: 99999 !important;
    pointer-events: none !important;
    backdrop-filter: blur(10px) !important;
}

/* ============================================
   SNAKE GAME SCALING - SLIDE PADDING FIX
   ============================================ */
.slide {
    padding: clamp(2vh, 2.5vh, 3vh) clamp(2.5vw, 3vw, 3.5vw) clamp(8vh, 10vh, 12vh) clamp(2.5vw, 3vw, 3.5vw) !important;
    overflow-y: auto !important;
    overflow-x: hidden !important;
    overscroll-behavior: contain !important;
}

.title-slide {
    padding: clamp(4vh, 5vh, 6vh) clamp(3vw, 4vw, 5vw) !important;
}

/* Make sure content doesn't overlap navigation */
.slide-container {
    padding-bottom: clamp(70px, 10vh, 100px) !important;
}

/* ============================================
   SNAKE GAME SCALING - RESPONSIVE H2
   ============================================ */
h2 {
    font-size: clamp(1.4rem, 2.8vw, 2.2rem) !important;
    margin-bottom: clamp(1vh, 1.5vh, 2vh) !important;
    padding-bottom: clamp(0.5vh, 0.8vh, 1vh) !important;
}

/* ============================================
   SNAKE GAME SCALING - REDUCE ALL GAPS
   ============================================ */
.crisis-stat-large {
    margin-bottom: clamp(1vh, 1.5vh, 2vh) !important;
    padding: clamp(0.5vh, 1vh, 1.5vh) 0 !important;
}

.crisis-number-huge {
    font-size: clamp(3rem, 6vw, 5rem) !important;
    margin-bottom: clamp(0.5vh, 1vh, 1.5vh) !important;
}

.crisis-label-large {
    font-size: clamp(0.85rem, 1.6vw, 1.1rem) !important;
}

.failure-flow {
    gap: clamp(1.5vw, 2vw, 2.5vw) !important;
    margin: clamp(1vh, 1.5vh, 2vh) auto !important;
}

.failure-step {
    padding: clamp(1.5vh, 2vh, 2.5vh) clamp(1vw, 1.5vw, 2vw) !important;
}

.failure-icon {
    font-size: clamp(1.8rem, 3vw, 2.5rem) !important;
    margin-bottom: clamp(0.5vh, 1vh, 1.5vh) !important;
}

.failure-step h3 {
    font-size: clamp(1rem, 1.8vw, 1.3rem) !important;
    margin-bottom: clamp(0.5vh, 1vh, 1.5vh) !important;
}

.failure-step p {
    font-size: clamp(0.8rem, 1.4vw, 0.95rem) !important;
}

.insight-box {
    padding: clamp(1.5vh, 2vh, 2.5vh) clamp(1.5vw, 2vw, 2.5vw) !important;
    margin-top: clamp(1vh, 1.5vh, 2vh) !important;
}

.innovation-section {
    margin-bottom: clamp(1vh, 1.5vh, 2vh) !important;
    padding: clamp(1vh, 1.5vh, 2vh) clamp(1.5vw, 2vw, 2.5vw) !important;
}

.platform-grid {
    gap: clamp(1vh, 1.5vh, 2vh) clamp(1.5vw, 2vw, 2.5vw) !important;
    margin-top: clamp(1vh, 1.5vh, 2vh) !important;
    max-height: none !important;
}

.platform-item {
    padding: clamp(1.5vh, 2vh, 2.5vh) clamp(1vw, 1.5vw, 2vw) !important;
}

.platform-item .platform-icon {
    font-size: clamp(1.5rem, 2.5vw, 2rem) !important;
    margin-bottom: clamp(0.5vh, 1vh, 1.5vh) !important;
}

.platform-item h4 {
    font-size: clamp(0.95rem, 1.7vw, 1.2rem) !important;
    margin-bottom: clamp(0.3vh, 0.5vh, 0.8vh) !important;
}

.platform-item p {
    font-size: clamp(0.75rem, 1.3vw, 0.9rem) !important;
}

.genius-callout {
    padding: clamp(1vh, 1.5vh, 2vh) clamp(1.5vw, 2vw, 2.5vw) !important;
    margin-top: clamp(1vh, 1.5vh, 2vh) !important;
    font-size: clamp(0.95rem, 1.7vw, 1.2rem) !important;
}

.component-explanation {
    gap: clamp(2vw, 2.5vw, 3vw) !important;
    margin: clamp(1vh, 1.5vh, 2vh) 0 !important;
}

.component-left {
    padding: clamp(1.5vh, 2vh, 2.5vh) clamp(1vw, 1.5vw, 2vw) !important;
}

.component-stat .stat-value {
    font-size: clamp(2.5rem, 5vw, 4rem) !important;
    margin-bottom: clamp(0.5vh, 1vh, 1.5vh) !important;
}

.component-stat .stat-label {
    font-size: clamp(0.95rem, 1.7vw, 1.2rem) !important;
    margin-bottom: clamp(1vh, 1.5vh, 2vh) !important;
}

.component-desc {
    font-size: clamp(0.9rem, 1.6vw, 1.1rem) !important;
}

.component-right h3 {
    font-size: clamp(1rem, 1.8vw, 1.3rem) !important;
    margin-bottom: clamp(0.5vh, 1vh, 1.5vh) !important;
}

.not-list, .is-list {
    margin-bottom: clamp(1vh, 1.5vh, 2vh) !important;
}

.not-list li, .is-list li {
    margin-bottom: clamp(0.5vh, 0.8vh, 1vh) !important;
    font-size: clamp(0.8rem, 1.4vw, 0.95rem) !important;
}

.analogy-box {
    padding: clamp(1vh, 1.5vh, 2vh) clamp(1.5vw, 2vw, 2.5vw) !important;
    margin-top: clamp(1vh, 1.5vh, 2vh) !important;
    font-size: clamp(0.9rem, 1.6vw, 1.1rem) !important;
}

.budget-visual {
    gap: clamp(0.8vh, 1vh, 1.2vh) !important;
    margin: clamp(1vh, 1.5vh, 2vh) auto !important;
}

.budget-bar {
    height: clamp(18px, 2.2vh, 25px) !important;
}

.budget-footer {
    padding: clamp(1vh, 1.5vh, 2vh) clamp(1.5vw, 2vw, 2.5vw) !important;
    margin-top: clamp(1vh, 1.5vh, 2vh) !important;
    font-size: clamp(0.85rem, 1.5vw, 1rem) !important;
}

.timeline-phases {
    gap: clamp(1.5vh, 2vh, 2.5vh) clamp(1.5vw, 2vw, 2.5vw) !important;
    margin: clamp(1vh, 1.5vh, 2vh) auto !important;
}

.phase-card {
    padding: clamp(1.5vh, 2vh, 2.5vh) clamp(1vw, 1.5vw, 2vw) !important;
}

.phase-header {
    margin-bottom: clamp(0.5vh, 1vh, 1.5vh) !important;
}

.phase-number {
    width: clamp(30px, 3.5vw, 40px) !important;
    height: clamp(30px, 3.5vw, 40px) !important;
    font-size: clamp(0.95rem, 1.7vw, 1.2rem) !important;
}

.phase-title {
    font-size: clamp(1rem, 1.8vw, 1.4rem) !important;
}

.phase-timeline {
    font-size: clamp(0.75rem, 1.3vw, 0.9rem) !important;
    margin-bottom: clamp(0.3vh, 0.5vh, 0.8vh) !important;
}

.phase-budget {
    font-size: clamp(1.1rem, 2vw, 1.5rem) !important;
    margin-bottom: clamp(0.5vh, 1vh, 1.5vh) !important;
}

.phase-milestones li {
    margin-bottom: clamp(0.5vh, 0.8vh, 1vh) !important;
    font-size: clamp(0.7rem, 1.2vw, 0.85rem) !important;
}

.validation-grid {
    gap: clamp(1.5vh, 2vh, 2.5vh) clamp(1.5vw, 2vw, 2.5vw) !important;
    margin: clamp(1vh, 1.5vh, 2vh) auto !important;
}

.validation-card {
    padding: clamp(1.5vh, 2vh, 2.5vh) clamp(1.5vw, 2vw, 2.5vw) !important;
}

.validation-header {
    margin-bottom: clamp(1vh, 1.5vh, 2vh) !important;
}

.validation-card li {
    margin-bottom: clamp(0.5vh, 1vh, 1.5vh) !important;
    font-size: clamp(0.75rem, 1.3vw, 0.9rem) !important;
}

.validation-footer {
    padding: clamp(1vh, 1.5vh, 2vh) clamp(1.5vw, 2vw, 2.5vw) !important;
    margin-top: clamp(1vh, 1.5vh, 2vh) !important;
    font-size: clamp(0.9rem, 1.6vw, 1.1rem) !important;
}

.impact-metrics {
    gap: clamp(1.5vh, 2vh, 2.5vh) !important;
    margin: clamp(1vh, 1.5vh, 2vh) auto !important;
}

.impact-row {
    gap: clamp(1.5vw, 2vw, 2.5vw) !important;
}

.impact-metric {
    padding: clamp(1.5vh, 2vh, 2.5vh) clamp(1vw, 1.5vw, 2vw) !important;
    gap: clamp(0.5vh, 1vh, 1.5vh) !important;
}

.impact-icon {
    font-size: clamp(1.5rem, 2.5vw, 2rem) !important;
    margin-bottom: clamp(0.3vh, 0.5vh, 0.8vh) !important;
}

.impact-value {
    font-size: clamp(1.3rem, 2.5vw, 2rem) !important;
}

.impact-label {
    font-size: clamp(0.75rem, 1.3vw, 0.9rem) !important;
}

.impact-statement {
    padding: clamp(1.5vh, 2vh, 2.5vh) clamp(1.5vw, 2vw, 2.5vw) !important;
    margin-top: clamp(1vh, 1.5vh, 2vh) !important;
}

.impact-statement p {
    font-size: clamp(0.9rem, 1.6vw, 1.1rem) !important;
}

.why-gauteng {
    gap: clamp(1.5vh, 2vh, 2.5vh) !important;
    margin: clamp(1vh, 1.5vh, 2vh) 0 !important;
}

.why-section {
    padding: clamp(1.5vh, 2vh, 2.5vh) clamp(1.5vw, 2vw, 2.5vw) !important;
}

.why-section h3 {
    font-size: clamp(1.1rem, 2vw, 1.5rem) !important;
    margin-bottom: clamp(1vh, 1.5vh, 2vh) !important;
}

.why-grid {
    gap: clamp(1vh, 1.5vh, 2vh) clamp(1.5vw, 2vw, 2.5vw) !important;
}

.why-item {
    padding: clamp(1vh, 1.5vh, 2vh) clamp(1vw, 1.5vw, 2vw) !important;
}

.why-item strong {
    font-size: clamp(0.9rem, 1.6vw, 1.1rem) !important;
    margin-bottom: clamp(0.3vh, 0.5vh, 0.8vh) !important;
}

.why-item p {
    font-size: clamp(0.75rem, 1.3vw, 0.9rem) !important;
}

.urgency-list {
    gap: clamp(1vh, 1.2vh, 1.5vh) !important;
}

.urgency-item {
    gap: clamp(1vw, 1.5vw, 2vw) !important;
    padding: clamp(1vh, 1.5vh, 2vh) clamp(1vw, 1.5vw, 2vw) !important;
}

.urgency-item i {
    font-size: clamp(1.3rem, 2.2vw, 1.8rem) !important;
}

.urgency-item strong {
    font-size: clamp(0.9rem, 1.6vw, 1.1rem) !important;
    margin-bottom: clamp(0.2vh, 0.3vh, 0.5vh) !important;
}

.urgency-item p {
    font-size: clamp(0.75rem, 1.3vw, 0.9rem) !important;
}

.value-prop {
    padding: clamp(1.5vh, 2vh, 2.5vh) clamp(1.5vw, 2vw, 2.5vw) !important;
    margin-top: clamp(1vh, 1.5vh, 2vh) !important;
    font-size: clamp(0.95rem, 1.7vw, 1.15rem) !important;
}

.ask-layout {
    gap: clamp(2vw, 2.5vw, 3vw) !important;
    margin: clamp(1vh, 1.5vh, 2vh) 0 !important;
}

.ask-main h1 {
    font-size: clamp(2rem, 4vw, 3rem) !important;
    margin-bottom: clamp(1vh, 1.5vh, 2vh) !important;
}

.ask-amount-box {
    padding: clamp(1.5vh, 2vh, 2.5vh) clamp(1.5vw, 2vw, 2.5vw) !important;
    margin-bottom: clamp(1vh, 1.5vh, 2vh) !important;
}

.ask-amount {
    font-size: clamp(2rem, 4vw, 3rem) !important;
}

.ask-duration {
    font-size: clamp(0.95rem, 1.7vw, 1.2rem) !important;
}

.ask-outcomes h3 {
    font-size: clamp(1.1rem, 2vw, 1.5rem) !important;
    margin-bottom: clamp(1vh, 1.5vh, 2vh) !important;
}

.outcome-grid {
    gap: clamp(0.5vh, 1vh, 1.5vh) !important;
    margin-bottom: clamp(1vh, 1.5vh, 2vh) !important;
}

.outcome-item {
    padding: clamp(0.8vh, 1vh, 1.5vh) clamp(1vw, 1.5vw, 2vw) !important;
}

.outcome-item span {
    font-size: clamp(0.8rem, 1.4vw, 0.95rem) !important;
}

.ask-sequencing {
    gap: clamp(1vw, 1.5vw, 2vw) !important;
    margin: clamp(1vh, 1.5vh, 2vh) 0 !important;
}

.sequence-step {
    padding: clamp(1vh, 1.5vh, 2vh) clamp(1vw, 1.5vw, 2vw) !important;
    max-width: clamp(150px, 20vw, 180px) !important;
}

.ubuntu-quote-box {
    padding: clamp(1.5vh, 2vh, 2.5vh) clamp(1.5vw, 2vw, 2.5vw) !important;
    margin-top: clamp(1vh, 1.5vh, 2vh) !important;
}

.ubuntu-quote-box p {
    font-size: clamp(0.95rem, 1.7vw, 1.2rem) !important;
    margin: clamp(0.5vh, 1vh, 1.5vh) 0 !important;
}

.ask-sidebar {
    padding: clamp(1.5vh, 2vh, 2.5vh) clamp(1vw, 1.5vw, 2vw) !important;
}

.contact-info-compact h4 {
    font-size: clamp(0.95rem, 1.7vw, 1.2rem) !important;
    margin-bottom: clamp(0.5vh, 1vh, 1.5vh) !important;
    margin-top: clamp(1vh, 1.5vh, 2vh) !important;
}

.contact-info-compact p {
    font-size: clamp(0.75rem, 1.3vw, 0.9rem) !important;
    margin-bottom: clamp(0.3vh, 0.5vh, 0.8vh) !important;
}

.ask-logo {
    max-width: clamp(100px, 15vw, 120px) !important;
    margin-bottom: clamp(1vh, 1.5vh, 2vh) !important;
}

.final-cta {
    padding: clamp(1.5vh, 2vh, 2.5vh) clamp(1.5vw, 2vw, 2.5vw) !important;
    margin-top: clamp(1vh, 1.5vh, 2vh) !important;
    font-size: clamp(1rem, 1.8vw, 1.3rem) !important;
}

.title-grid {
    gap: clamp(1.5vh, 2vh, 2.5vh) clamp(2vw, 2.5vw, 3vw) !important;
    margin: 0 auto clamp(2vh, 2.5vh, 3vh) auto !important;
}

.title-box {
    padding: clamp(1.5vh, 2vh, 2.5vh) clamp(1.5vw, 2vw, 2.5vw) !important;
}

.title-value {
    font-size: clamp(2rem, 4vw, 3.5rem) !important;
}

.presenter-info {
    font-size: clamp(0.8rem, 1.4vw, 0.95rem) !important;
}
/* ============================================
   EMERGENCY LANDSCAPE FIX - AGGRESSIVE REDUCTION
   ============================================ */

/* CRITICAL: Hide overflow completely */
.slide {
    padding: 1.5vh 2.5vw 8vh 2.5vw !important;
    overflow: hidden !important;
    overflow-y: hidden !important;
    justify-content: center !important;
}

.title-slide {
    padding: 2vh 3vw !important;
    overflow: hidden !important;
}

/* TITLE SLIDE - OPTIMIZED FOR LANDSCAPE */
.logo-container {
    margin-bottom: clamp(20px, 3vh, 35px) !important;
}

.logo-img {
    max-width: clamp(180px, 25vw, 280px) !important;
    max-height: clamp(180px, 25vw, 280px) !important;
}

.title-slide h1 {
    font-size: clamp(2rem, 4vw, 3.5rem) !important;
    margin-bottom: clamp(10px, 1.5vh, 20px) !important;
    letter-spacing: -1px !important;
    line-height: 1.1 !important;
}

.title-slide .subtitle {
    font-size: clamp(0.9rem, 1.6vw, 1.2rem) !important;
    margin-bottom: clamp(15px, 2.5vh, 30px) !important;
}

.title-grid {
    gap: clamp(1vh, 1.5vh, 2vh) clamp(2vw, 2.5vw, 3vw) !important;
    margin: 0 auto clamp(15px, 2vh, 25px) auto !important;
    max-width: 800px !important;
}

.title-box {
    padding: clamp(1vh, 1.5vh, 2vh) clamp(1.5vw, 2vw, 2.5vw) !important;
}

.title-label {
    font-size: clamp(0.7rem, 1.2vw, 0.85rem) !important;
    margin-bottom: 0.3vh !important;
}

.title-value {
    font-size: clamp(1.8rem, 3.5vw, 2.8rem) !important;
    margin-bottom: 0.3vh !important;
}

.title-desc {
    font-size: clamp(0.7rem, 1.2vw, 0.85rem) !important;
}

.presenter-info {
    font-size: clamp(0.7rem, 1.2vw, 0.85rem) !important;
    margin-top: clamp(10px, 1.5vh, 20px) !important;
}

.presenter-info p {
    margin: 0.3vh 0 !important;
}

/* ALL OTHER SLIDES - AGGRESSIVE REDUCTION */
h2 {
    font-size: clamp(1.2rem, 2.2vw, 1.8rem) !important;
    margin-bottom: 1vh !important;
    padding-bottom: 0.5vh !important;
}

/* Crisis slide */
.crisis-number-huge {
    font-size: clamp(2.5rem, 5vw, 4rem) !important;
    margin-bottom: 0.5vh !important;
}

.crisis-label-large {
    font-size: clamp(0.75rem, 1.4vw, 1rem) !important;
}

.failure-flow {
    gap: 1.5vw !important;
    margin: 1vh auto !important;
}

.failure-step {
    padding: 1.5vh 1vw !important;
    max-width: 250px !important;
}

.failure-icon {
    font-size: clamp(1.5rem, 2.5vw, 2rem) !important;
    margin-bottom: 0.5vh !important;
}

.failure-step h3 {
    font-size: clamp(0.9rem, 1.6vw, 1.2rem) !important;
    margin-bottom: 0.5vh !important;
}

.failure-step p {
    font-size: clamp(0.7rem, 1.3vw, 0.9rem) !important;
}

.failure-arrow {
    font-size: clamp(1.5rem, 2.5vw, 2rem) !important;
}

.insight-box {
    padding: 1.5vh 2vw !important;
    margin-top: 1vh !important;
}

.insight-box i {
    font-size: clamp(1.5rem, 2.5vw, 2rem) !important;
}

.insight-text {
    font-size: clamp(0.85rem, 1.5vw, 1rem) !important;
}

/* Platform grid - MORE COMPACT */
.innovation-section {
    margin-bottom: 1vh !important;
    padding: 1vh 1.5vw !important;
}

.innovation-section h3 {
    font-size: clamp(1.1rem, 2vw, 1.5rem) !important;
    margin-bottom: 0.5vh !important;
}

.innovation-desc {
    font-size: clamp(0.8rem, 1.4vw, 1rem) !important;
}

.platform-grid {
    gap: 1vh 1.5vw !important;
    margin-top: 1vh !important;
}

.platform-item {
    padding: 1.5vh 1vw !important;
}

.platform-item .platform-icon {
    font-size: clamp(1.3rem, 2.2vw, 1.8rem) !important;
    margin-bottom: 0.5vh !important;
}

.platform-item h4 {
    font-size: clamp(0.85rem, 1.5vw, 1.1rem) !important;
    margin-bottom: 0.3vh !important;
}

.platform-item p {
    font-size: clamp(0.7rem, 1.2vw, 0.85rem) !important;
}

.genius-callout {
    padding: 1vh 2vw !important;
    margin-top: 1vh !important;
    font-size: clamp(0.85rem, 1.5vw, 1.1rem) !important;
}

/* Component explanation */
.component-explanation {
    gap: 2vw !important;
    margin: 1vh 0 !important;
}

.component-left {
    padding: 1.5vh 1vw !important;
}

.component-stat .stat-value {
    font-size: clamp(2rem, 4vw, 3rem) !important;
    margin-bottom: 0.5vh !important;
}

.component-stat .stat-label {
    font-size: clamp(0.85rem, 1.5vw, 1.1rem) !important;
    margin-bottom: 1vh !important;
}

.component-desc {
    font-size: clamp(0.8rem, 1.4vw, 1rem) !important;
}

.component-right h3 {
    font-size: clamp(0.95rem, 1.7vw, 1.2rem) !important;
    margin-bottom: 0.5vh !important;
}

.not-list li, .is-list li {
    margin-bottom: 0.5vh !important;
    font-size: clamp(0.7rem, 1.3vw, 0.9rem) !important;
}

.analogy-box {
    padding: 1vh 2vw !important;
    margin-top: 1vh !important;
    font-size: clamp(0.8rem, 1.4vw, 1rem) !important;
}

/* Budget - MORE COMPACT */
.budget-visual {
    gap: 0.8vh !important;
    margin: 1vh auto !important;
}

.budget-category {
    font-size: clamp(0.75rem, 1.3vw, 0.9rem) !important;
}

.budget-amount {
    font-size: clamp(0.85rem, 1.5vw, 1.1rem) !important;
}

.budget-bar {
    height: clamp(16px, 2vh, 22px) !important;
}

.budget-percent {
    font-size: clamp(0.7rem, 1.2vw, 0.85rem) !important;
}

.budget-footer {
    padding: 1vh 1.5vw !important;
    margin-top: 1vh !important;
    font-size: clamp(0.75rem, 1.3vw, 0.95rem) !important;
}

/* Timeline phases */
.timeline-phases {
    gap: 1.5vh 1.5vw !important;
    margin: 1vh auto !important;
}

.phase-card {
    padding: 1.5vh 1vw !important;
}

.phase-header {
    margin-bottom: 0.5vh !important;
}

.phase-number {
    width: clamp(28px, 3vw, 35px) !important;
    height: clamp(28px, 3vw, 35px) !important;
    font-size: clamp(0.85rem, 1.5vw, 1.1rem) !important;
}

.phase-title {
    font-size: clamp(0.95rem, 1.7vw, 1.3rem) !important;
}

.phase-timeline {
    font-size: clamp(0.7rem, 1.2vw, 0.85rem) !important;
    margin-bottom: 0.3vh !important;
}

.phase-budget {
    font-size: clamp(1rem, 1.8vw, 1.4rem) !important;
    margin-bottom: 0.5vh !important;
}

.phase-milestones li {
    margin-bottom: 0.5vh !important;
    font-size: clamp(0.65rem, 1.1vw, 0.8rem) !important;
}

/* Validation grid */
.validation-grid {
    gap: 1.5vh 1.5vw !important;
    margin: 1vh auto !important;
}

.validation-card {
    padding: 1.5vh 1.5vw !important;
}

.validation-header {
    margin-bottom: 1vh !important;
}

.validation-header i {
    font-size: clamp(1.3rem, 2.2vw, 1.8rem) !important;
}

.validation-header h3 {
    font-size: clamp(0.9rem, 1.6vw, 1.2rem) !important;
}

.validation-card li {
    margin-bottom: 0.5vh !important;
    font-size: clamp(0.7rem, 1.2vw, 0.85rem) !important;
}

.validation-footer {
    padding: 1vh 2vw !important;
    margin-top: 1vh !important;
    font-size: clamp(0.8rem, 1.4vw, 1rem) !important;
}

/* Impact metrics */
.impact-metrics {
    gap: 1.5vh !important;
    margin: 1vh auto !important;
}

.impact-row {
    gap: 1.5vw !important;
}

.impact-metric {
    padding: 1.5vh 1vw !important;
    gap: 0.5vh !important;
    max-width: 220px !important;
}

.impact-icon {
    font-size: clamp(1.3rem, 2.2vw, 1.8rem) !important;
    margin-bottom: 0.3vh !important;
}

.impact-value {
    font-size: clamp(1.2rem, 2.2vw, 1.8rem) !important;
}

.impact-label {
    font-size: clamp(0.7rem, 1.2vw, 0.85rem) !important;
}

.impact-statement {
    padding: 1.5vh 2vw !important;
    margin-top: 1vh !important;
}

.impact-statement p {
    font-size: clamp(0.8rem, 1.4vw, 1rem) !important;
}

/* Why Gauteng */
.why-gauteng {
    gap: 1.5vh !important;
    margin: 1vh 0 !important;
}

.why-section {
    padding: 1.5vh 1.5vw !important;
}

.why-section h3 {
    font-size: clamp(1rem, 1.8vw, 1.4rem) !important;
    margin-bottom: 1vh !important;
}

.why-grid {
    gap: 1vh 1.5vw !important;
}

.why-item {
    padding: 1vh 1vw !important;
}

.why-item strong {
    font-size: clamp(0.8rem, 1.4vw, 1rem) !important;
    margin-bottom: 0.3vh !important;
}

.why-item p {
    font-size: clamp(0.7rem, 1.2vw, 0.85rem) !important;
}

.urgency-list {
    gap: 1vh !important;
}

.urgency-item {
    gap: 1vw !important;
    padding: 1vh 1vw !important;
}

.urgency-item i {
    font-size: clamp(1.2rem, 2vw, 1.6rem) !important;
}

.urgency-item strong {
    font-size: clamp(0.8rem, 1.4vw, 1rem) !important;
    margin-bottom: 0.2vh !important;
}

.urgency-item p {
    font-size: clamp(0.7rem, 1.2vw, 0.85rem) !important;
}

.value-prop {
    padding: 1.5vh 2vw !important;
    margin-top: 1vh !important;
    font-size: clamp(0.85rem, 1.5vw, 1.05rem) !important;
}

/* The Ask */
.ask-layout {
    gap: 2vw !important;
    margin: 1vh 0 !important;
}

.ask-main h1 {
    font-size: clamp(1.8rem, 3.5vw, 2.5rem) !important;
    margin-bottom: 1vh !important;
}

.ask-amount-box {
    padding: 1.5vh 1.5vw !important;
    margin-bottom: 1vh !important;
}

.ask-amount {
    font-size: clamp(1.8rem, 3.5vw, 2.5rem) !important;
}

.ask-duration {
    font-size: clamp(0.85rem, 1.5vw, 1.1rem) !important;
}

.ask-outcomes h3 {
    font-size: clamp(1rem, 1.8vw, 1.4rem) !important;
    margin-bottom: 1vh !important;
}

.outcome-grid {
    gap: 0.5vh !important;
    margin-bottom: 1vh !important;
}

.outcome-item {
    padding: 0.8vh 1vw !important;
}

.outcome-item span {
    font-size: clamp(0.75rem, 1.3vw, 0.9rem) !important;
}

.ask-sequencing {
    gap: 1vw !important;
    margin: 1vh 0 !important;
}

.sequence-step {
    padding: 1vh 1vw !important;
    max-width: clamp(130px, 18vw, 160px) !important;
}

.sequence-label {
    font-size: clamp(0.75rem, 1.3vw, 0.9rem) !important;
    margin-bottom: 0.3vh !important;
}

.sequence-action {
    font-size: clamp(0.75rem, 1.3vw, 0.9rem) !important;
}

.ubuntu-quote-box {
    padding: 1.5vh 2vw !important;
    margin-top: 1vh !important;
}

.ubuntu-quote-box i {
    font-size: clamp(1rem, 1.8vw, 1.4rem) !important;
}

.ubuntu-quote-box p {
    font-size: clamp(0.85rem, 1.5vw, 1.1rem) !important;
    margin: 0.5vh 0 !important;
}

.ask-sidebar {
    padding: 1.5vh 1vw !important;
}

.contact-info-compact h4 {
    font-size: clamp(0.85rem, 1.5vw, 1.1rem) !important;
    margin-bottom: 0.5vh !important;
    margin-top: 1vh !important;
}

.contact-info-compact p {
    font-size: clamp(0.7rem, 1.2vw, 0.85rem) !important;
    margin-bottom: 0.3vh !important;
}

.ask-logo {
    max-width: clamp(90px, 13vw, 110px) !important;
    margin-bottom: 1vh !important;
}

.partner-badge {
    padding: 0.6vh 0.8vw !important;
    font-size: clamp(0.7rem, 1.2vw, 0.8rem) !important;
    margin-bottom: 0.3vh !important;
}

.final-cta {
    padding: 1.5vh 2vw !important;
    margin-top: 1vh !important;
    font-size: clamp(0.95rem, 1.7vw, 1.2rem) !important;
}

/* NAVIGATION - FORCE ABOVE EVERYTHING */
.navigation {
    z-index: 999999 !important;
    pointer-events: auto !important;
}

.nav-btn {
    pointer-events: auto !important;
    cursor: pointer !important;
    z-index: 999999 !important;
}

.nav-btn:disabled {
    opacity: 0.3 !important;
    cursor: not-allowed !important;
}

.slide-counter {
    z-index: 999999 !important;
}

/* Force no scrollbars */
body {
    overflow: hidden !important;
}

.slide-container {
    overflow: hidden !important;
}

/* ============================================
   LOGO SIZE FIX - 50% BIGGER
   ============================================ */
.logo-img {
    max-width: clamp(120px, 18vw, 180px) !important;
    max-height: clamp(120px, 18vw, 180px) !important;
}

/* ============================================
   LOGO SIZE - MAKE EVEN BIGGER
   ============================================ */
.logo-img {
    max-width: clamp(180px, 25vw, 280px) !important;
    max-height: clamp(180px, 25vw, 280px) !important;
}

.logo-container {
    margin-bottom: clamp(20px, 3vh, 35px) !important;
}

/* ============================================
   BUTTON - FORCE CLICKABLE
   ============================================ */
.nav-btn {
    pointer-events: auto !important;
    cursor: pointer !important;
    position: relative !important;
    z-index: 9999999 !important;
    touch-action: manipulation !important;
}

.nav-btn:disabled {
    opacity: 0.3 !important;
    cursor: not-allowed !important;
    pointer-events: none !important;
}

.navigation {
    pointer-events: auto !important;
    z-index: 9999999 !important;
    touch-action: manipulation !important;
}
/* ============================================
   SLIDE 10 - ULTRA COMPACT TO FIT LANDSCAPE
   ============================================ */

.ask-layout {
    gap: 1.5vw !important;
    margin: 0.5vh 0 !important;
}

.ask-main h1 {
    font-size: clamp(1.5rem, 3vw, 2rem) !important;
    margin-bottom: 0.5vh !important;
}

.ask-amount-box {
    padding: 1vh 1.5vw !important;
    margin-bottom: 0.8vh !important;
}

.ask-amount {
    font-size: clamp(1.6rem, 3vw, 2.2rem) !important;
}

.ask-duration {
    font-size: clamp(0.8rem, 1.4vw, 1rem) !important;
}

.ask-outcomes h3 {
    font-size: clamp(0.95rem, 1.7vw, 1.2rem) !important;
    margin-bottom: 0.8vh !important;
}

.outcome-grid {
    gap: 0.4vh !important;
    margin-bottom: 0.8vh !important;
}

.outcome-item {
    padding: 0.6vh 1vw !important;
}

.outcome-item i {
    font-size: clamp(0.8rem, 1.4vw, 1rem) !important;
}

.outcome-item span {
    font-size: clamp(0.7rem, 1.2vw, 0.85rem) !important;
}

.ask-sequencing {
    gap: 0.8vw !important;
    margin: 0.8vh 0 !important;
}

.sequence-step {
    padding: 0.8vh 1vw !important;
    max-width: clamp(110px, 16vw, 140px) !important;
}

.sequence-label {
    font-size: clamp(0.7rem, 1.2vw, 0.85rem) !important;
    margin-bottom: 0.2vh !important;
}

.sequence-action {
    font-size: clamp(0.7rem, 1.2vw, 0.85rem) !important;
}

.sequence-arrow {
    font-size: clamp(1.2rem, 2vw, 1.6rem) !important;
}

.ubuntu-quote-box {
    padding: 1vh 1.5vw !important;
    margin-top: 0.8vh !important;
}

.ubuntu-quote-box i {
    font-size: clamp(0.9rem, 1.6vw, 1.2rem) !important;
}

.ubuntu-quote-box p {
    font-size: clamp(0.8rem, 1.4vw, 1rem) !important;
    margin: 0.4vh 0 !important;
}

.ask-sidebar {
    padding: 1vh 1vw !important;
}

.contact-info-compact h4 {
    font-size: clamp(0.8rem, 1.4vw, 1rem) !important;
    margin-bottom: 0.4vh !important;
    margin-top: 0.8vh !important;
}

.contact-info-compact h4:first-child {
    margin-top: 0 !important;
}

.contact-info-compact p {
    font-size: clamp(0.65rem, 1.1vw, 0.8rem) !important;
    margin-bottom: 0.2vh !important;
}

.ask-logo {
    max-width: clamp(80px, 12vw, 100px) !important;
    margin-bottom: 0.8vh !important;
}

.partners-compact {
    margin-top: 0.8vh !important;
}

.partner-badge {
    padding: 0.5vh 0.8vw !important;
    font-size: clamp(0.65rem, 1.1vw, 0.75rem) !important;
    margin-bottom: 0.2vh !important;
}

.final-cta {
    padding: 1vh 1.5vw !important;
    margin-top: 0.8vh !important;
    font-size: clamp(0.85rem, 1.5vw, 1.1rem) !important;
}

/* Slide 10 specific padding reduction */
.slide.final-slide {
    padding: 1vh 2vw 8vh 2vw !important;
}

/* ============================================
   SLIDE 3 - WHITE ICONS (not green on green)
   ============================================ */
.platform-item .platform-icon {
    color: #FFFFFF !important;
}

.platform-item .platform-icon i {
    color: #FFFFFF !important;
}

/* ============================================
   ILLUSTRATION PLACEHOLDERS (v79)
   ============================================ */
.illustration-placeholder {
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
    border-radius: 15px;
}

.illustration-placeholder img {
    width: 100%;
    height: auto;
    opacity: 0.9;
    transition: opacity 0.4s ease, transform 0.4s ease;
}

.illustration-placeholder img:hover {
    opacity: 1;
    transform: scale(1.05);
}

/* Graceful degradation if SVG missing */
.illustration-placeholder img[src$=".svg"]:not([src]) {
    display: none;
}

/* ============================================
   v81 - NAVIGATION & LAYOUT FIXES
   ============================================ */

/* Ensure slides don't overflow and have proper bottom spacing */
.slide {
    max-height: 100vh !important;
    padding-bottom: clamp(10vh, 12vh, 14vh) !important;
    overflow-y: auto !important;
    overflow-x: hidden !important;
}

/* Navigation must always be clickable */
.navigation {
    position: fixed !important;
    bottom: clamp(20px, 3vh, 30px) !important;
    right: clamp(20px, 3vw, 30px) !important;
    z-index: 99999999 !important;
    pointer-events: auto !important;
}

.nav-btn {
    z-index: 99999999 !important;
    pointer-events: auto !important;
    position: relative !important;
}

/* Thumbnail nav hidden by default, shown only in fullscreen */
.thumbnail-nav {
    display: none !important;
}

/* When in fullscreen, show thumbnail nav */
:fullscreen .thumbnail-nav {
    display: flex !important;
}

/* Slide counter */
.slide-counter {
    position: fixed !important;
    bottom: clamp(20px, 3vh, 30px) !important;
    left: clamp(20px, 3vw, 30px) !important;
    z-index: 99999999 !important;
    pointer-events: none !important;
}

/* Ensure no background elements block navigation */
.visual-story-bg,
.mega-icon,
.illustration-placeholder {
    z-index: -1 !important;
    pointer-events: none !important;
}

/* Story cards need to be visible and interactive */
.story-card {
    z-index: 5 !important;
    pointer-events: auto !important;
}

/* But allow clicks on clickable elements within slides */
.stat-card,
.platform-item,
.loan-item,
.revenue-card {
    z-index: 1 !important;
    pointer-events: auto !important;
}

/* ============================================
   v82 - HERO TITLE SLIDE REDESIGN
   ============================================ */

/* Gradient background */
.hero-gradient {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: radial-gradient(ellipse at top, rgba(76, 175, 80, 0.15) 0%, transparent 50%),
                radial-gradient(ellipse at bottom right, rgba(76, 175, 80, 0.1) 0%, transparent 60%);
    z-index: 0;
    pointer-events: none;
}

/* Hero container */
.hero-container {
    position: relative;
    z-index: 1;
    max-width: 1200px;
    margin: 0 auto;
    height: 100%;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    padding: 2vh 3vw;
}

/* Logo */
.hero-logo {
    margin-bottom: clamp(2vh, 3vh, 4vh);
}

.hero-logo img {
    max-width: clamp(140px, 20vw, 220px);
    filter: drop-shadow(0 10px 40px rgba(76, 175, 80, 0.3));
}

/* Main Title */
.hero-title {
    text-align: center;
    margin-bottom: clamp(3vh, 4vh, 5vh);
}

.hero-main {
    font-size: clamp(3rem, 7vw, 6rem) !important;
    font-weight: 900;
    line-height: 1.1;
    letter-spacing: -2px;
    margin-bottom: clamp(1vh, 1.5vh, 2vh);
    text-transform: uppercase;
    background: linear-gradient(135deg, #fff 0%, #4CAF50 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
}

.hero-highlight {
    color: #4CAF50;
    text-shadow: 0 0 40px rgba(76, 175, 80, 0.6);
    -webkit-text-fill-color: #4CAF50;
}

.hero-subtitle {
    font-size: clamp(1.1rem, 2vw, 1.6rem);
    color: #bbb;
    font-weight: 400;
    letter-spacing: 0.5px;
}

/* Investment Boxes */
.hero-boxes {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: clamp(2vw, 3vw, 4vw);
    width: 100%;
    max-width: 900px;
    margin-bottom: clamp(3vh, 4vh, 5vh);
}

.hero-box {
    background: linear-gradient(135deg, rgba(76, 175, 80, 0.08), rgba(76, 175, 80, 0.03));
    border: 2px solid rgba(76, 175, 80, 0.3);
    border-radius: 20px;
    padding: clamp(2vh, 2.5vh, 3vh) clamp(2vw, 2.5vw, 3vw);
    text-align: center;
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    position: relative;
    overflow: hidden;
}

.hero-box::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(76, 175, 80, 0.1), transparent);
    transition: left 0.5s ease;
}

.hero-box:hover::before {
    left: 100%;
}

.hero-box:hover {
    transform: translateY(-8px) scale(1.02);
    border-color: #4CAF50;
    box-shadow: 0 20px 60px rgba(76, 175, 80, 0.4);
}

.hero-box-primary {
    border-left: 4px solid #4CAF50;
}

.hero-box-secondary {
    border-left: 4px solid rgba(76, 175, 80, 0.6);
}

.hero-box-icon {
    font-size: clamp(2rem, 4vw, 3rem);
    color: #4CAF50;
    margin-bottom: clamp(1vh, 1.5vh, 2vh);
    opacity: 0.8;
}

.hero-box-label {
    font-size: clamp(0.85rem, 1.5vw, 1.1rem);
    color: #999;
    text-transform: uppercase;
    letter-spacing: 1.5px;
    font-weight: 600;
    margin-bottom: clamp(0.5vh, 1vh, 1.5vh);
}

.hero-box-value {
    font-size: clamp(2.5rem, 5vw, 4rem);
    font-weight: 900;
    color: #4CAF50;
    line-height: 1;
    margin-bottom: clamp(0.5vh, 1vh, 1.5vh);
    text-shadow: 0 0 20px rgba(76, 175, 80, 0.3);
}

.hero-box-desc {
    font-size: clamp(0.85rem, 1.5vw, 1.1rem);
    color: #ccc;
    line-height: 1.4;
}

/* Tagline */
.hero-tagline {
    font-size: clamp(1.2rem, 2.5vw, 2rem);
    color: #fff;
    font-weight: 300;
    font-style: italic;
    text-align: center;
    margin-bottom: clamp(3vh, 4vh, 5vh);
    padding: clamp(1.5vh, 2vh, 2.5vh) clamp(2vw, 3vw, 4vw);
    background: rgba(76, 175, 80, 0.05);
    border-radius: 50px;
    border: 1px solid rgba(76, 175, 80, 0.2);
    max-width: 800px;
}

/* Footer */
.hero-footer {
    text-align: center;
    font-size: clamp(0.85rem, 1.5vw, 1.1rem);
    color: #999;
}

.hero-presenter {
    margin-bottom: clamp(0.5vh, 1vh, 1.5vh);
}

.hero-presenter strong {
    color: #4CAF50;
    font-weight: 600;
}

.hero-context {
    color: #777;
    font-size: clamp(0.8rem, 1.4vw, 1rem);
}

/* ============================================
   v83 - LANDSCAPE-OPTIMIZED HERO (16:9)
   ============================================ */

/* Override previous hero styles */
.hero-gradient,
.hero-container,
.hero-logo,
.hero-title,
.hero-main,
.hero-boxes,
.hero-box,
.hero-tagline,
.hero-footer {
    display: none !important;
}

/* New Landscape Hero */
.hero-landscape {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: clamp(4vw, 6vw, 8vw);
    align-items: center;
    max-width: 1400px;
    margin: 0 auto;
    height: 100%;
    padding: 0 4vw;
}

/* Left Side */
.hero-left {
    display: flex;
    flex-direction: column;
    justify-content: center;
    gap: clamp(1.5vh, 2vh, 2.5vh);
}

.hero-logo-img {
    max-width: clamp(100px, 12vw, 140px);
    margin-bottom: clamp(1vh, 1.5vh, 2vh);
}

.hero-title-text {
    font-size: clamp(2.5rem, 5vw, 4.5rem) !important;
    font-weight: 900;
    line-height: 0.95;
    letter-spacing: -1px;
    margin: 0 !important;
    text-transform: uppercase;
}

.hero-validation {
    color: #4CAF50;
    display: block;
}

.hero-company {
    font-size: clamp(1rem, 1.8vw, 1.3rem);
    color: #999;
    font-weight: 500;
    margin-top: clamp(0.5vh, 1vh, 1.5vh);
}

.hero-tagline-compact {
    font-size: clamp(0.95rem, 1.6vw, 1.2rem);
    color: #4CAF50;
    font-style: italic;
    margin-top: clamp(1vh, 1.5vh, 2vh);
    opacity: 0.8;
}

.hero-tagline-compact i {
    opacity: 0.4;
    font-size: 0.8em;
    margin: 0 0.5em;
}

/* Right Side */
.hero-right {
    display: flex;
    flex-direction: column;
    gap: clamp(2vh, 2.5vh, 3vh);
}

.hero-box-compact {
    background: linear-gradient(135deg, rgba(76, 175, 80, 0.1), rgba(76, 175, 80, 0.05));
    border: 2px solid rgba(76, 175, 80, 0.3);
    border-radius: 15px;
    padding: clamp(2vh, 2.5vh, 3vh) clamp(2vw, 2.5vw, 3vw);
    transition: all 0.3s ease;
}

.hero-box-compact:hover {
    transform: translateX(8px);
    border-color: #4CAF50;
    box-shadow: 0 10px 30px rgba(76, 175, 80, 0.3);
}

.hero-primary {
    border-left: 4px solid #4CAF50;
}

.hero-secondary {
    border-left: 4px solid rgba(76, 175, 80, 0.6);
}

.hero-box-header {
    display: flex;
    align-items: center;
    gap: 1vw;
    margin-bottom: clamp(1vh, 1.5vh, 2vh);
    font-size: clamp(0.75rem, 1.3vw, 0.95rem);
    color: #999;
    text-transform: uppercase;
    letter-spacing: 1.5px;
    font-weight: 600;
}

.hero-box-header i {
    font-size: clamp(1.2rem, 2vw, 1.6rem);
    color: #4CAF50;
}

.hero-amount {
    font-size: clamp(2.5rem, 5vw, 4rem);
    font-weight: 900;
    color: #4CAF50;
    line-height: 1;
    margin-bottom: clamp(0.5vh, 1vh, 1.5vh);
}

.hero-detail {
    font-size: clamp(0.85rem, 1.5vw, 1.1rem);
    color: #ccc;
    line-height: 1.3;
}

.hero-presenter-compact {
    text-align: right;
    font-size: clamp(0.8rem, 1.4vw, 1rem);
    color: #777;
    padding-top: clamp(1vh, 1.5vh, 2vh);
    border-top: 1px solid rgba(76, 175, 80, 0.2);
}

.hero-presenter-compact strong {
    color: #4CAF50;
}

/* ============================================
   v84 - GLOBAL 16:9 LANDSCAPE ENFORCEMENT
   ============================================ */

/* Force all slides to fit in viewport without scrolling */
.slide {
    max-height: 100vh !important;
    overflow: hidden !important;
    padding: clamp(2vh, 3vh, 4vh) clamp(3vw, 4vw, 5vw) clamp(8vh, 9vh, 10vh) clamp(3vw, 4vw, 5vw) !important;
    display: flex !important;
    flex-direction: column !important;
    justify-content: center !important;
}

/* Reduce all heading sizes for landscape */
.slide h2 {
    font-size: clamp(1.5rem, 3vw, 2.2rem) !important;
    margin-bottom: clamp(2vh, 2.5vh, 3vh) !important;
}

.slide h3 {
    font-size: clamp(1.1rem, 2vw, 1.5rem) !important;
    margin-bottom: clamp(1vh, 1.5vh, 2vh) !important;
}

/* Compact all grids to single row */
.stats-grid,
.platform-grid,
.validation-grid,
.revenue-grid,
.success-grid,
.risk-grid {
    display: grid !important;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)) !important;
    gap: clamp(1.5vw, 2vw, 2.5vw) !important;
    max-height: 60vh !important;
}

/* Hide all illustration placeholders to save vertical space */
.illustration-placeholder {
    display: none !important;
}

/* Compact all cards */
.stat-card,
.platform-item,
.loan-item,
.revenue-card,
.validation-card,
.success-card,
.risk-item,
.team-card {
    padding: clamp(1.5vh, 2vh, 2.5vh) clamp(1.5vw, 2vw, 2.5vw) !important;
}

/* Story cards - make horizontal instead of vertical */
.story-card {
    flex-direction: row !important;
    padding: clamp(1.5vh, 2vh, 2.5vh) clamp(2vw, 2.5vw, 3vw) !important;
    margin: clamp(1vh, 1.5vh, 2vh) 0 !important;
}

.story-card-icon {
    font-size: clamp(2rem, 3vw, 2.5rem) !important;
    min-width: 60px !important;
}

/* Failure flow - make horizontal */
.failure-flow {
    display: flex !important;
    flex-direction: row !important;
    align-items: center !important;
    gap: clamp(2vw, 3vw, 4vw) !important;
    margin: clamp(2vh, 3vh, 4vh) 0 !important;
}

.failure-step {
    flex: 1 !important;
    padding: clamp(1.5vh, 2vh, 2.5vh) clamp(1.5vw, 2vw, 2.5vw) !important;
}

.failure-arrow {
    font-size: clamp(2rem, 4vw, 3rem) !important;
}

/* Crisis stat - reduce size */
.crisis-stat-large {
    margin: clamp(2vh, 3vh, 4vh) 0 !important;
}

.crisis-number-huge {
    font-size: clamp(3rem, 6vw, 5rem) !important;
}

/* Budget bars - compact */
.budget-visual {
    max-height: 50vh !important;
    overflow-y: auto !important;
}

.budget-bar-item {
    margin-bottom: clamp(1vh, 1.5vh, 2vh) !important;
}

/* Timeline phases - horizontal */
.timeline-phases {
    display: grid !important;
    grid-template-columns: 1fr 1fr 1fr !important;
    gap: clamp(2vw, 3vw, 4vw) !important;
}

.phase-card {
    padding: clamp(1.5vh, 2vh, 2.5vh) clamp(1.5vw, 2vw, 2.5vw) !important;
}

.phase-milestones li {
    font-size: clamp(0.75rem, 1.3vw, 0.9rem) !important;
    margin-bottom: 0.5vh !important;
}

/* Impact metrics - single row */
.impact-metrics {
    max-height: 55vh !important;
}

.impact-row {
    display: flex !important;
    gap: clamp(1.5vw, 2vw, 2.5vw) !important;
    margin-bottom: clamp(1.5vh, 2vh, 2.5vh) !important;
}

.impact-metric {
    flex: 1 !important;
    padding: clamp(1.5vh, 2vh, 2.5vh) clamp(1vw, 1.5vw, 2vw) !important;
}

/* Loan breakdown - 2 rows max */
.loan-breakdown {
    display: grid !important;
    grid-template-columns: repeat(3, 1fr) !important;
    gap: clamp(1.5vw, 2vw, 2.5vw) !important;
}

/* Platform grid - single row */
.platform-grid {
    grid-template-columns: repeat(6, 1fr) !important;
}

/* Tech stack - compact */
.tech-stack {
    max-height: 55vh !important;
    overflow-y: auto !important;
}

.tech-layer {
    margin-bottom: clamp(1vh, 1.5vh, 2vh) !important;
    padding: clamp(1vh, 1.5vh, 2vh) clamp(1.5vw, 2vw, 2.5vw) !important;
}

/* Revenue grid - 2 rows */
.revenue-grid {
    grid-template-columns: repeat(3, 1fr) !important;
}

/* Risk grid - single row if possible */
.risk-grid {
    grid-template-columns: repeat(2, 1fr) !important;
}

/* Success grid - 2 rows */
.success-grid {
    grid-template-columns: repeat(3, 1fr) !important;
}

/* Component explanation - horizontal */
.component-explanation {
    display: grid !important;
    grid-template-columns: 1fr 1fr !important;
    gap: clamp(3vw, 4vw, 5vw) !important;
}

/* Why grid - single row */
.why-grid {
    grid-template-columns: repeat(4, 1fr) !important;
    gap: clamp(1.5vw, 2vw, 2.5vw) !important;
}

.why-item {
    padding: clamp(1vh, 1.5vh, 2vh) clamp(1.5vw, 2vw, 2.5vw) !important;
}

/* Urgency list - horizontal */
.urgency-list {
    display: grid !important;
    grid-template-columns: 1fr 1fr !important;
    gap: clamp(2vw, 2.5vw, 3vw) !important;
}

.urgency-item {
    padding: clamp(1vh, 1.5vh, 2vh) clamp(1.5vw, 2vw, 2.5vw) !important;
}
